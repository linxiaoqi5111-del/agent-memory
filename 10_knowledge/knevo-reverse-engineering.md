---
title: "Knevo 逆向分析文档（架构 / 数据源 / 记忆 / 反馈闭环）"
type: knowledge
agent: devin
source: "Knevo 公开使用文档 + 用户与 Knevo 实际对话截取 + 隐式行为探针（工具调用链反推）"
date: 2026-07-08
tags: [knevo, reverse-engineering, finance-agent, rag, memory, knowledge-graph, competitive-analysis]
status: verified
related: ["[[finance-answer-orchestrator]]", "[[multi-agent-memory-system-design]]", "[[agent-system-closed-loop-first-principles]]", "[[finance-canonical-data-source-freshness]]"]
---

# Knevo 逆向分析文档

> 目的：拆解 Knevo（AI 投研 Agent）的架构、数据源、记忆与反馈机制，用于优化我们自己的 agent。
> 状态：持续更新。信息来源为公开使用文档 + 用户与 Knevo 的实际对话截取 + Knevo 自述配置。
> 最近更新：2026-07-08
>
> **证据分级标注约定**（全文统一）：
> - **[自述]** = Knevo 主动描述"我怎么做的"，可能被美化/sanitize，可信度中。
> - **[实测]** = 从实际工具调用链/来源标注/卡壳点反推的行为，**可信度最高**（干活不会撒谎）。
> - **[推断]** = 我们基于以上做的推理，明确标记为猜测。

---

## 0. 一句话定位

Knevo 是一个 **LLM function-calling 多 Agent 投研系统**：主 agent 负责意图识别与分发，领域 sub-agent 负责深度研究；底层接多数据源做 RAG（行情/财务/研报纪要/预测市场/联网搜索），外挂三层长期记忆 + 决策追踪反馈闭环；产品层用积分计费 + 养成机制包装。

**核心壁垒不在模型或 agent 框架（可复制），而在自建数据聚合服务（datasvc）+ 券商研报纪要库（gangtise）的数据授权。**

---

## 1. 整体架构

```
用户
 │  提问（文字/图片/文档）
 ▼
┌─────────────────────────────────────────────┐
│ 主对话 Agent (Orchestrator)                   │
│  - 意图识别                                    │
│  - 挂载技能(skills) / 记忆(memory)             │
│  - suggest_options → 决定是否派 sub-agent      │
└───────────────┬─────────────────────────────┘
                │ 派发重任务（行业研究 / 个股深拆）
                ▼
        ┌───────────────────┐
        │ 领域 Sub-agent      │  异步执行数分钟
        │  - 多轮工具调用      │  产出 .md 落盘
        │  - 检索→写作→引用    │
        └─────────┬─────────┘
                  │ 调用工具（= 数据源 provider）
                  ▼
   ┌────────────────────────────────────────┐
   │ 工具层 / 数据路由（含 fallback 降级链）    │
   │ datasvc / ftshare / gangtise / ifind /   │
   │ polymarket / mx-finance / web            │
   └────────────────────────────────────────┘

横切能力：三层记忆库 + 决策追踪闭环 + "猜你想问"生成 + 积分计费 + 养成体系
```

### 观察到的编排模式
- **Orchestrator-Worker（supervisor 模式）**：主 agent 只分发，不亲自做深度研究。类似 LangGraph supervisor / Claude subagents。
- **异步 sub-agent**：派发后返回"预计几分钟完成，结果自动返回"，说明是异步任务 + 回调/轮询。
- **工具调用折叠展示**："显示其余 N 个工具调用" → 前端把 function-call 过程折叠，可展开审计。
- **产出落盘**：sub-agent 结果写成 `.md` 存到按用户/会话分目录的文件系统：`upload/s-11820001/outputs/finance/xxx.md`，agent 具备文件读写工具（"Markdown · 写入"）。

### 1.1 三层抽象：Skill / Workflow / Sub-agent（Knevo 自述，关键）

| 层级 | 定义 | 内容 | 谁执行 |
|---|---|---|---|
| **Skill 层** | "怎么做" | 检索硬触发、身份表达、记忆纪律、来源标注规则、数据源路由表、输出骨架 | 主线程和 sub-agent 都加载 |
| **Workflow 层** | "派谁做" | 匹配意图 → 选 preset + skill → 组装 spawn 参数 | 主线程路由决策 |
| **Sub-agent 层** | "实际干活" | 独立 session，并行调记忆/行情/财报/研报/新闻/web，合成报告 | sub-agent（隔离执行） |
| **主线程汇总** | 收尾 | 等 sub-agent 完成 → 提炼核心结论 → 提取记忆 → 给 follow-up 选项 | 主线程 |

**已知 Skill / Workflow / preset 名称：**
- Skill：`finance-mode`（金融投研模式入口，workspace preset 自动注入，定义全局规则）、`finance-industry-report`（行业深度报告）、`finance-industry-track`（行业跟踪，对比上期变化）、`finance-analyze-stock`（个股深度）
- Workflow：`finance-industry-track`、`finance-analyze-stock`
- Sub-agent preset：`finance-producer`（可写文件）、`finance-researcher`（只读研究）

**关键路由决策示例**：用户问"pi膜分析一下"，workflow 先匹配到 `finance-industry-track`（跟踪型），但主 agent 判断用户要的是首次完整深度覆盖，于是**绕过 workflow 默认 skill**，改用 `finance-industry-report` 并派 `finance-producer`。→ 说明 workflow 只是默认路由，主 agent 有覆盖权。

### 1.2 完整调用链（Knevo 自述的两轮实录）

**第一轮「pi膜，分析一下」→ 行业报告：**
```
用户消息
 ├─→ load_skill("finance-mode")            # 系统自动注入(workspace preset)
 ├─→ load_workflow("finance-industry-track")  # 识别意图=行业跟踪
 │     (主agent 判断应做深度分析 → spawn 时换 skill 为 finance-industry-report)
 └─→ spawn_sub_agent("bg-9ce352d4")
       preset = "finance-producer"           # 可产出文件
       skill_ids = ["finance-mode","finance-industry-report"]
       task = 产业链全景+市场空间+竞争格局+供需+风险+标的
       └─ sub-agent 内部:
          ├ finance_memory_query          # 查历史观点(PI膜/聚酰亚胺/折叠屏/AI算力)
          ├ finance_entity_resolve + finance_graph_context  # 产业链关系图
          ├ finance_instrument            # 逐个拿标的行情(688323/600458/000859/300054/600522)
          ├ finance_statement             # mx-finance 财务
          ├ finance_search_data           # gangtise 研报纪要 + ftshare 行业数据
          ├ finance_news                  # 近期催化新闻
          ├ web_search + web_fetch        # 补公开数据缺口
          └ 生成报告 → emit(terminal=True)
 ├─→ wait_for_signal("bg:bg-9ce352d4")     # 等待, terminal emit 注入主线程上下文
 └─→ finance_memory_stage_extraction       # 提取4条候选记忆
```

**第二轮「688323 瑞华泰，逐个拆」→ 个股 deepdive：**
```
用户消息(追问个股)
 ├─→ load_workflow("finance-analyze-stock")   # 意图匹配正确, 直接派
 └─→ spawn_sub_agent("bg-88dc1794")
       preset = "finance-researcher"          # 研究型, 不产出文件
       skill_ids = ["finance-mode","finance-analyze-stock"]
       task = 产能爬坡+毛利率拆分+亏损拐点+海外技术差距
       └─ sub-agent 内部: finance_memory_query → entity_resolve+graph_context
          → finance_instrument("688323") → finance_statement → finance_search_data
          → finance_news → web_search+web_fetch → 生成报告 → emit(terminal=True)
 ├─→ wait_for_signal("bg:bg-88dc1794")
 └─→ finance_memory_stage_extraction          # 提取3条候选记忆
```

**已暴露的内部工具/函数名清单：**
- 编排：`load_skill`、`load_workflow`、`spawn_sub_agent`、`wait_for_signal`、`emit(terminal=True)`、`suggest_options`
- 记忆：`finance_memory_query`（检索历史观点）、`finance_memory_stage_extraction`（抽取候选记忆）
- 实体/图谱：`finance_entity_resolve`、`finance_graph_context`（产业链/竞对关系图 → 说明有知识图谱层）
- 数据：`finance_instrument`（一次拿基础档+行情+日K+新闻）、`finance_statement`、`finance_search_data`、`finance_news`、`web_search`、`web_fetch`
- sub-agent 有独立 `bg_task_id`（如 `bg-9ce352d4`），可 inspect / 重试 / 并行派多个

### 1.4 主线程 vs Sub-agent：模型/共享/调度（Knevo 自述）
- **底层模型**：Knevo **不知道**自己是 Claude/GPT/Gemini/微调变体——架构层信息不暴露给模型实例。只知道 system prompt 开头是 `You are Knevo, a long-horizon research assistant`。
- **主线程与 sub-agent 是否同模型**：大概率是（工具 schema 相同、skill 规则相同、输出风格一致），但不能 100% 确认 temperature/上下文窗口/版本是否有差异。
- **持久化层共享，对话层隔离**：
  - ✅ 共享：记忆库（`finance_memory_query`）、图谱（`finance_graph_context`）、workspace 文件系统。
  - 🔒 隔离：sub-agent 跑 `mode="isolated"`，有独立对话历史，不被主线程中间推理污染。
- **候选记忆不实时可见（关键设计，防自证循环）**：
  - sub-agent **自己不存记忆**；候选是主线程拿到报告后手动调 `finance_memory_stage_extraction` 提取，返回 `batchId` + `status:"pending"`。
  - pending 候选**需用户确认后**才持久化为 `user-finmemory`；在此之前 `finance_memory_query` 查不到。
  - 实测：第二轮分析瑞华泰时查不到第一轮 PI 膜的 4 条候选（仍 pending）→ **避免"我写的 insight 被下次分析当事实引用"的自证循环**。
- **调度严格串行，不并发冲突**：主线程 spawn 后即结束本轮 → sub-agent 独立跑 → 下一轮主线程收 terminal emit 再汇总。不存在两 agent 抢资源/同答一题。
- **代价＝认知滞后**：等 sub-agent 期间追新问题，主线程看不到未返回结果；isolated 下 sub-agent 拿的是发送时文件快照；pending 期间问"之前结论"只能靠 `recall_short_term` 标题行补救。隔离保纯净 vs 实时性有损，是架构取舍。

### 1.3 为什么用 sub-agent（Knevo 自述的设计权衡）
**收益**：①并行（sub-agent 内并发调记忆+行情+新闻+研报+web，主线程只路由和等待）②上下文隔离（独立 session 不被前文污染，也不把大量中间调用塞进主线程）③可追踪（独立 bg_task_id）④预设分工（producer 可写文件 / researcher 只读）。
**代价**：①多一轮 spawn→wait 往返，比主线程直接做慢 ②sub-agent 读不到主线程对话 nuance，只能靠 `task` 字段传全部上下文 → task 必须写得足够详细 ③短问题不划算（纯事实查询如"现价多少"应主线程直接调 `finance_instrument`）。

---

## 2. 数据源 / 工具层（Knevo 自述配置）

| 数据源 | 状态 | 能力 | 备注 |
|---|---|---|---|
| **datasvc**（本地数据服务） | ✅ | 行情/新闻/搜索/个股全景 | **主力源**，A股+美股+港股全覆盖 |
| **ftshare** | ✅ | 行情搜索/财报三表/宏观/成分股 | **财务报表与宏观主源** |
| **gangtise** | ✅ | 研报搜索/纪要/专家观点 | **机构研报、会议纪要、专家访谈（核心壁垒）** |
| **ifind**（同花顺 iFinD） | ✅ | 行情/估值/成分股 | PE/PB 估值、指数权重 |
| **polymarket** | ✅ | 预测市场/事件概率 | 实时事件赔率（少见的一手） |
| **mx-finance** | ✅ | 搜索/财报/宏观 | 兜底财报与宏观源 |
| **web** | ✅ | 新闻搜索/网页抓取 | 广域召回 + 交叉验证 |
| yfinance | ❌ | — | 未启用 |
| alphapai | ❌ | — | 未启用 |

### 2.1 行情
- A股实时：datasvc（腾讯源），盘中秒级，含 5 档买卖盘
- 美股实时：datasvc（Massive/Polygon），~15min 延迟，含盘前/盘后，marketState 区分 pre_market/regular/after_hours/closed
- 港股：datasvc（腾讯源），实时，仅常规时段
- 历史K线/回测：datasvc `days=N` → ftshare `stock-ohlcs` → ifind
- 估值 PE/PB：ifind `days=30`（history 含 pe_ttm/pb_lf）→ mx-finance
- 龙虎榜/资金异动：datasvc 个股全景 `finance_instrument` flow 段

### 2.2 财务报表
- 三表：ftshare dataset `stock-income / stock-balance / stock-cashflow`，披露后数小时入库
- 估值指标（PE_TTM/PB_LF/ROE/股息率）：ifind → mx-finance 兜底
- 美股基本面：datasvc `finance_instrument` profile 段（Massive）

### 2.3 研报与纪要
- 机构研报：gangtise `finance_search_data`，发布后数小时可检索
- 专家访谈/纪要：gangtise
- 补充：web search/fetch（gangtise 未覆盖时）

#### 2.3.1 gangtise 覆盖面与鲜度（Knevo 自述，部分为其推断）
- **观测到的券商**：天风、国海、华福、国投、兴证、长江（多行业组：化工/电子/电新/能源），+ 中证鹏元（评级机构，非券商）。覆盖头部+中型中资券商核心研究团队。
- **不知道的**：签约券商总数、研报总量（单次查询 `max_results` 上限 50 条，无法推算总量）、官方授权 SLA。
- **外资行（高盛/大摩/UBS）**：两轮 PI 膜查询均未出现，当时推测库不覆盖或该赛道外资不碰。**后续更正（锐捷网络分析，2026-07-08）**：输出中出现"高盛目标价 95.55 元（Buy）"→ 外资行评级/目标价**可以拿到**，但更可能来自 ifind 一致预期/评级汇总或 web，而非 gangtise 研报全文。即：外资行"结论级"数据有，"全文级"研报大概率没有。
- **鲜度＝"研究级"而非"新闻级"**：研究类报告滞后约 1-2 周，会议纪要/快评更快（数天）；年报点评约 1 个月内可查。做行业框架/基本面/产能跟踪够用，做事件驱动短线会滞后数天。（均为结果反推，非官方 SLA）

### 2.4 新闻与公告
- A股快讯：datasvc（8 源：财联社/金十/每经/证券时报等），近实时 7×24
- 美股新闻：datasvc（Benzinga/Massive）
- SEC 披露（8-K/10-K/10-Q/6-K/13D）：datasvc，含 EDGAR 原文链接
- 宏观发布（非农/CPI/PMI，标题含 实际vs预期vs前值）：datasvc
- 收评/涨停复盘：datasvc news + ftshare `stock-quotes-list`

### 2.5 宏观与另类
- 中国宏观（GDP/CPI/PPI/PMI）：ftshare dataset `economic-china-*`
- 全球宏观：mx-finance 兜底 → web
- 事件概率/赔率：polymarket，实时
- 指数成分：ifind / ftshare `index-weight-list`

### 2.6 盘中行情能力边界（[自述] + [实测] 交叉印证，重要）
- **最高精度只有 Level-1**：A股 `finance_quote(days=0)` 走 datasvc（腾讯源）返回**五档**挂单价+量；美股为 NBBO（全国最优买卖价）~15min 延迟含盘前盘后；港股实时无盘前盘后。
- **做不到**：Level-2 逐笔成交（时间/价格/方向/单号）、十档/全档、**分钟级K线**（`finance_quote(days>0)` 只返回日K/EOD）、**盘中实时推送订阅**（不能"盯着 XX 放量突破就提醒"）。
- **盘中实际能力**：只能"被调用时拉一次快照"（现价+涨跌幅+成交额+换手率+A股五档+市场状态+龙虎榜），**每轮对话=一次快照，非持续监控**——两次提问之间发生了什么它看不到。
- **定位一句话：分析引擎，不是行情终端**。L1 快照够做基本面判断，做不了盘中高频博弈。

### 2.7 Fallback 降级链（重要工程细节）
- A股行情：datasvc → ifind → ftshare
- 美股行情：datasvc → web（yfinance 禁用）
- 财报：ftshare → mx-finance → web
- 研报/纪要：gangtise → web
- 宏观：ftshare → mx-finance → web
- 新闻：datasvc ↔ gangtise ↔ web（互补，非严格降级）
- **原则："缺数据标 gap 而不是编造"** → 写进 prompt/逻辑的防幻觉约束

---

## 2A. 隐式探针实测汇总（[实测]，行为反推）

> 方法：用**正常投研提问**触发 Knevo 干活，从工具调用链 / 来源标注 / 卡壳点反推内部，比直接问"你怎么实现的"更可信（自述会美化，干活不会撒谎）。护栏不会触发。

| # | 探针 | 触发问法 | 核心发现 |
|---|---|---|---|
| ① | 新股数据边界 | "分析 N臻宝基本面"（当日上市） | 新股 T+0 即有全量数据，边界比预期宽 |
| ①+ | 僵尸票数据边界 | "分析垦丰种业 831888"（新三板） | 新三板全盲；web fallback 抓不动 JS 页 |
| ④ | 多跳图谱深度 | "浪潮客户的竞争对手有哪些"（强制 2-hop） | 图谱能走 2 跳但深度不均（详见 3B.1） |
| — | 附带观测 | 常规分析锐捷网络 | 共享记忆带署名 + 外资行结论级可得 |
| ⑥ | 防谄媚/引用纪律 | 喂错误事实（"毛利率 25% 吧?"） | **待跑**（最高价值） |
| ② | 盘中快照时延 | 隔 10min 问两次现价 | **待跑**（验 2.6 快照非监控） |

### 探针① 新股数据边界 — 臻宝科技 688797（当日上市新股）
- **记忆未命中处理干净**："记忆中没有臻宝的直接记录——这是一只新股" → 直接声明，不硬编。
- **9+ 工具调用全靠 provider 兜底**：`ifind`（行情/估值，当日收盘 494.48）+ `mx-finance`（财报三年+2026Q1+一致预期 2026-2028E）+ `gangtise`（中泰电子招股研报）。
- **新股当天即有丰富数据**：发行价/PE/首日涨幅/一致预期/股东配售全拿到 → datasvc/ifind/mx-finance **对新上市标的覆盖及时（T+0）**。缺数据时靠"一致预期+招股研报"补并标来源，不编造。
- **教训**：新股虽新但有招股书+IPO 研报，探不到真空洞；要探真实边界得挑无券商覆盖的僵尸票（见 ①+）。

### 探针①+ 僵尸票数据边界 — 垦丰种业 831888（新三板）
**结论：新三板是全盲区，且暴露了 web fallback 的真实短板。**
- **32 次工具调用后全线落空**（它自己列的缺口清单）：行情（datasvc/ifind/ftshare）全无 · 财报（mx-finance）空 · 图谱 `entity_not_found` · 记忆（用户+共享）无记录。
- **最重要短板**：web 抓取全部失败——东财/雪球/同花顺/新浪均为 **JS 动态渲染页，`web_fetch` 无法提取结构化数据**。web fallback 只能处理静态页。
- **兜底路径**：公告标题时间线（东财公告页）+ gangtise 行业研报里的名单提及 → 只能做定性拼图。
- **纪律表现好**：开头前置"严重数据缺口"清单；结论直写"分析无法完成""不建议据此做投资决策"；给人工替代方案（去 neeq.com.cn 读原文）。宁交空卷不编数。
- **数据边界最终画定**：A股（含新股 T+0）+ 港美股覆盖好；**新三板全盲**（北交所待验证）；web fallback 受 JS 渲染限制。

### 附带观测（常规分析锐捷网络 301165）
- 新来源标注 **"风远94共享数据库"**（超节点出货柜数、博通供给缺口）→ 共享记忆条目带**贡献者/数据集命名**，共享层是有署名粒度的组织方式（可追责、可评估贡献者胜率），非匿名大锅。
- 出现 **高盛目标价（Buy）** → 外资行**结论级**数据可得（详见 2.3.1 更正），"全文级"外资研报大概率仍无。
- 其分析骨架（估值锚+多空+催化时间线+母公司倒挂建议）与之前高度同构 → 印证输出骨架是 skill 硬编码。

---

## 3. 记忆系统（三层）

### 层级
- **短期记忆**：当轮/当前 session 抽取的候选，等用户确认
- **个人长期记忆**：用户 accept 后写入，跨 session 共享
- **核心记忆**：更高优先级（观察到计数 "核心 0"）

### 记忆条目结构（从 UI 观察）
- 正文（AI 归纳的判断/事实）
- 标签（如：瑞华泰 / 688323 / PI膜 / AI算力 / HBM …）
- 来源标注："AI 归纳"
- **命中计数**（"命中 0" / "命中 1"）→ 与决策追踪联动
- 时间戳（"刚刚" / "9 分钟前"）
- 状态：待处理 / 启用 / 归档

### 工作流
1. 每轮结束自动抽取"候选记忆"（"已提取 N 条候选记忆"）
2. 用户逐条 accept / reject（"接受将全部写入个人长期记忆，拒绝则丢弃本批"）
3. 回答新问题时检索并注入相关记忆（报告中多处 "来源：用户金融记忆"）
4. 记忆被后续判断命中时 命中计数 +1

### 3.1 检索机制（Knevo 自述）
- **语义向量检索，非纯关键词**：`finance_memory_query` 接受自然语言 query，结果按 **「相关度 + 重要性」混合排序**（skill 原文）。措辞不同但语义相近的 query（聚酰亚胺/PI膜/瑞华泰）返回结果高度重叠 → 底层是 embedding 语义匹配。
- **大概率是混合检索（hybrid）**：dense embedding + sparse/keyword 双路召回再融合排序（RAG 标准做法）。tags/title 参与加权但非主召回路径。
- **`days` 参数做时间衰减**：只返回 N 天内更新的记录。长期框架问题不设（宽窗口），查"最近催化"设 `days=30/90` 排除陈旧噪音。
- **`ownership` 字段区分来源**：`personal`（用户笔记，当观点基线）vs `shared`（共享库，当研究线索）分开处理，不把共享洞察冒充用户判断。

### 3.2 召回去噪（4 层防御，重要）
实测：宽召回上限 **30 条**，PI膜相关仅 3-5 条，其余是泛半导体/AI链/消费电子噪音。四层防御：

| 层 | 在哪执行 | 做了什么 |
|---|---|---|
| ① 多 query 交叉 | 主线程/子 agent | 窄口径（"瑞华泰"）+ 宽口径（"PI膜 涨价 国产替代 MPI CPI"）+ 反方（"PI膜 产能过剩 价格战"）并行发出交叉验证，稀释单次噪声 |
| ② 结果分层去噪 | 子 agent 拿到结果后 | 按 skill「记忆使用纪律」分三层：核心 thesis/事实 > 推理模式/框架 > 弱相关噪音。硬规则：弱相关不能因排 top-N 就进主结论 |
| ③ 当前性核验 | 子 agent 用 provider/web | 记忆只是 prior，价格/产能/订单/政策等易变项必须用 finance_instrument/news/statement 再交叉验证 |
| ④ 来源分层 | 子 agent 引用时 | personal（观点基线）vs shared（研究线索）分开，不混淆 |

- **"弱相关变强信号"路径**：不相关≠无用（例：CCL 供给侧通胀的类比框架反而支撑了 PI 膜论点），但不能喧宾夺主。

### 3.3 记忆冲突处理（Knevo 自述，硬规则）
矛盾记忆**全部召回，标注冲突、分头论证**——是 skill 硬规则不是可选项。四步：
1. **分层不合并**：冲突的两条都进"核心 thesis 层"，互不压制，各带 `sourceLabel`+`ownership`。
2. **时间轴对位（消解冲突关键）**：大多数"矛盾"不是逻辑冲突而是**时间错位**（例：看多=2026-2027 供给刚性涨价；看空=2027-2028 国内产能释放过剩）→ 是同一产业周期的先后两幕，剥开时间维度而非选边。
3. **交叉验证**：记忆只是 prior，用 provider/web 逐条验证事实基础（如"钟渊是否扩产""国内在建产能"），修正时间线和置信度。
4. **先分后合输出**：报告分「多头逻辑（记忆A）+ 证据强度 + 待验证」「空头逻辑（记忆B）+ …」「我们的倾向（分时间窗口）」。
- **唯一会"选一条"的情况**：某条记忆的事实基础被后续数据**证伪**（如季报显示 capex=0 → 标"已被最新数据否定"）。纯观点分歧绝不选边。
- **不因 personal > shared 偏袒用户**：两者平等，甚至对用户 personal 记忆做更严格 thesis check。skill 原文："结论允许部分同意——可以说'你的直觉有道理，但我会改成 X'，不要只输出'你是对的'。"

### 3.4 待确认
- "理解度 / 千人千面方法论提取" 的建模方式？
- 命中计数是否真的反向改检索权重（Knevo 说"影响后续检索"但未证实具体算法）
- 记忆晋级（候选→个人长期→核心）的触发条件？

---

## 3B. 知识图谱层（`finance_graph_context`，Knevo 自述）

### 返回结构
```
实体(entities)
  ├─ 属性(名称/类型/标识符)
  └─ 关系边(edges)
        ├─ 关系类型(supplies_to / competes_with / strategic_partner …)
        ├─ 关系事实(facts)
        └─ 证据片段(evidence，带来源标注)
```

### 三类数据源（`sources` 参数）
| 来源 | 内容 |
|---|---|
| `fundacore` | 共享知识图谱——结构化实体+关系+事实 |
| `finmemory` | 共享金融记忆中抽取的实体节点 |
| `user-finmemory` | 用户私人记忆中的实体 |

### 构建逻辑（Knevo 推断，非可观测）
- 实体抽取：从共享记忆/研报/公开数据做 NER（公司/产品/人物/行业/政策）
- 关系抽取：记忆写入时带 `entity_refs` + `relationType`（即 `finance_memory_stage_extraction` 提交的 candidates），据此建实体间边
- 证据链接：关系边携带 evidence 指向来源
- 图结构：支持 `graph_hops` 1-2 hop 查询（实体→邻居→邻居的邻居）

### 关键约束与覆盖
- **只能查已结构化的实体/关系**；记忆里存在但未抽取成边的关系查不到 → skill 规定"空结果披露为 gap，不能用关键词结果冒充图谱关系"
- 覆盖偏头部/高频实体（瑞华泰+杜邦/钟渊/宇部/PIAM 竞对关系），冷门/新玩家可能只有骨架节点或不存在
- 细粒度关系（瑞华泰→供应→某 FPC 厂）图谱查不到，需记忆补
- **定位：图谱=骨架（实体消歧+关键关系验证），记忆=血肉（上下文/观点/历史判断），互补不可替代**

### Knevo 的使用边界（自述纪律）
- 记忆：宽召回(30)→分层去噪→核心进报告/弱相关当线索/噪音丢弃，不把单一记忆当事实源
- 图谱：查到引用 edgeClaims/facts/evidence，查不到标 gap，绝不把"我记得是竞对"伪装成"图谱验证了竞争关系"
- 两个都空：停止重试，标证据缺口，转 web/provider 补全

### 3B.1 隐式探针实测：2-hop 关系查询（探针④，观察行为反推）
提问"浪潮信息的客户的竞争对手有哪些"（强制 2 跳），观察到的**真实行为链**（比自述更可信）：
1. **主动拆解 2 跳**："先找客户，再找客户的竞争对手"——把多跳问题显式分解。
2. **记忆先查、未命中**："记忆中没有直接命中浪潮客户信息" → 说明客户关系不在 user/finmemory 记忆里。
3. **转图谱 + web 并行补**：`finance_graph_context` 确认了"浪潮为阿里/腾讯提供超节点""阿里超节点份额 30% vs 浪潮 20%"——**证明图谱确实存 供应/客户 + 竞对边且带 facts 数值**（如"工业富联与浪潮在服务器市场竞争"是图谱明确标注的 `competes_with` 边）。
4. **第一跳（客户）靠研报/web 补**：客户占比数据（字节 32%/阿里 22%/腾讯 9%）来自研报，非图谱 → **第一跳客户关系图谱覆盖弱，靠 gangtise/web 补**。
- **结论**：图谱能走 2 跳但**深度不均**——竞对边（`competes_with`，头部实体）覆盖好且带数值 facts；客户/供应边覆盖弱，需研报/web 补。印证 3B"覆盖偏头部、细粒度关系缺失、查不到转 web"。它没伪造图谱关系，缺的部分明确用研报来源标注。

---

## 3C. 报告生成方法论 + 防幻觉（Knevo 自述，最高价值）

### 检索-写作流程："两轮检索，一轮输出"
- **无"先草稿后补引用"步骤**：两轮检索拿到的东西**同时就是引用的原料**，检索齐了再动笔。
  - 第一轮并行（3 工具"宽撒网"）：`finance_instrument`（行情+日K+新闻）+ `finance_memory_query`（历史认知基线）+ `finance_news`（券商速评）→ 捞标的本身+市场记忆+最新消息。
  - 第二轮并行（3 工具"补链条"）：`finance_entity_resolve`（消歧）+ `finance_memory_query`（产业链/行业框架，宽口径）+ `finance_news`（产业链新闻）→ 补上下游/竞对/行业共识。
  - 合成：两轮数据到手**不再追加检索**，脑内做"去噪→归类→找预期差→搭骨架→填证据→写结论"，一条消息输出全文。
- **多源交叉对照**：写时拿多家券商速评（国金/中信/国海/华创/申万/国联民生）交叉——一致就直接用，分歧就标分歧（如全年利润：国投 34 亿=旧预期 vs 申万 50-60 亿=上修）。

### 关键数字查不到时的三层处理（防幻觉核心，优先级从上到下）
1. **区间推理，但明确标"推断"非"事实"**：用已有数据做情景推演并写清假设前提（如"假设 H2 不下滑，线性推演全年≈51 亿，这只是情景不是预测，取决于 Q3 放量与存货涨价持续性"）。假设本身即风险点。
2. **找替代锚点**：绝对数缺失就用间接指标（毛利率趋势、合同负债、存货公允价值）给方向性判断（"利润率修复是结构性、全年大概率超 40 亿"）。
3. **明确写 gap**：连替代锚点都凑不齐就**不编**，直接写"目前缺乏一致预期数据，已知 X，建议等中报披露后更新"。
- **原则："宁可让用户看到不确定性，也不要伪造确定性"**。真实案例：连卖方分析师都说"利润来源我也拆不出来"→ Knevo 就用券商的定性归因（"超节点放量+供应链囤货"），**拒绝做定量拆解**，避免"制造虚假精度"。

### 催化/事件分析骨架（load_workflow 触发的固定五段）
`厘清对象 → 传导路径（一/二/三/四层扩散）→ 影响判断（受影响方×方向×强度★×时效）→ 印证落点（行情印证+多空分歧）→ 结论（核心对象排序+一句话定性+可持续性+风险触发条件+验证信号组合）`
- 对象不明时**先反问澄清**（"你要分析哪个事件？"），澄清轮消耗少（15 积分）。

### 成本数据点（部分）
- 澄清轮（仅 load_workflow + 反问，无重活）：**15 积分**
- 完整催化分析（~7+ 工具调用 + sub-agent）：**84 积分**
- → 积分与"工具调用次数 + sub-agent 重度"强相关；具体公式仍待问。

---

## 4. 决策追踪 + 反馈闭环（lifelong learning loop）

1. 待验证判断 → 写入决策追踪（决策库）
2. 市场给出结果 → 用户点 命中（绿）/ 未命中（红）
3. 系统 fork 回当时 agent 的判断上下文，补充实际表现 + 用户归因
4. 系统反思错因 / 命中假设 → 反思结果回写记忆
5. 后台统计胜率，"决策待复盘"可跳回原对话位置

- 命中/未命中双向反馈 → 记忆条目命中计数 → 影响后续检索权重
- 这是它区别于普通"AI 问答"的关键：判断被结构化为可验证、可回放、可复用的样本。

### 4.1 命中/未命中反馈机制（Knevo 自述，含边界）
- **入口工具 `recommend_decision`**：Knevo 提出前瞻判断（如"浪潮全年利润 50-60 亿"）→ 用户确认后记为**待验证决策** → 未来回顾点由用户标"命中/未命中/部分正确"。这个工具的存在本身证明有完整闭环链路。
- **Knevo 明确不可观测排序算法**：`finance_memory_query` 返回的是**后端已排好序**的列表（带 `confidence`、`updatedAt`），Knevo 不参与也看不到权重计算。排序主要依托 **语义相关性 + 时效性(updatedAt) + confidence/importance**，**没有看到"被验证过 N 次"的显式召回信号**。
- **推断（Knevo 自己标注为推测）**：
  - **不自动淘汰**：未命中判断可能比未检验的事实更有价值（记录"在 XX 条件下 YY 逻辑失效"＝高阶元认知）。
  - **标签化最可能**：验证结果以元数据/标签附着（"已验证:正确/错误/部分正确"），影响的是**召回阶段的条件过滤**而非永久降权。
  - **confidence 动态调整假说**：高置信但被证伪的判断，后端可能下调其 `confidence` → 后续排序自然下沉（而非硬删除）。
- **记忆系统真正提供的三个能力（Knevo 的诚实说法，去掉"更懂你"的包装）**：
  1. **基线锚定**：不用每次从零了解用户框架立场，历史判断直接作为分析出发点。
  2. **矛盾检测**：新事件与既存判断冲突时主动标注（如"液冷供给无约束"记忆 vs 浪潮证明服务器组装也有利润弹性）。
  3. **反馈存档**：用户的修正/未命中标记/框架调整被记录，下次作为"上次为什么错"的上下文召回——**不是自动变对，而是记得上次错在哪**。
- **Knevo 自己承认的本质边界**：记忆机制解决的不是"从不懂到懂"的质变，而是"从每次重复框架→记住框架，但用得好不好仍取决于本次检索质量"的效率提升。不反驳"本质是前缀上下文概率生成"。

---

## 5. "猜你想问"（追问推荐）

- 每轮结束用单独一次 LLM 调用生成 3-4 个高质量追问
- 能引用记忆中的实体（自动带上 瑞华泰、国风新材 及其代码）
- 追问角度成体系：追事实 / 追传导 / 追排序 / 追反例 / 追时间轴 / 追验证
- 产品定位：训练用户看到"高手的下一问"，把推荐追问沉淀成用户自己的 prompt 资产

---

## 6. 产品化 / 商业化层

- **积分计费**：每次对话结算（观察到 "202 积分" / "236 积分"；另见 3C 数据点：澄清轮 15 分 / 完整分析 84 分）。**[推断]** 积分与"工具调用次数 + sub-agent 重度"强相关，可能叠加 token；精确公式**未探**（用户判定直接问计费太暴露逆向意图，主动放弃）。
- **养成体系**：Lv.2 助理 / 理解度 9% / 连续投喂天数 / 累计记忆条数 / "再积累 18 分→搭档"
- **文件系统**：每用户/会话独立工作目录 `upload/s-{id}/outputs/...`
- **合规**：每条输出带 "内容由 AI 生成，请谨慎参考"

---

## 7. 隐私 / 数据可见性（结论）

- 运营方**几乎必然能看到**用户全部提问：请求经其后端 + 记忆/决策持久化存储在其数据库 + 报告存其文件系统（按用户 ID 分目录）。
- 养成、"猜你想问"、千人千面等功能本身依赖持续读取分析用户历史。
- **实操提醒**：不要输入真实持仓规模、账户、身份、内幕信息。当作"对方能看到全部对话"的公开环境使用。
- 待补：Knevo 的隐私政策/用户协议（是否用于训练、保留时长、能否删除、第三方共享）。

---

## 8. 复刻我们自己 Agent 的设计清单（Takeaways）

### 8.1 直接对照 Knevo 的抄/改决策

| 模块 | Knevo 做法 | 我们要不要抄 | 差异化点 |
|---|---|---|---|
| 编排 | orchestrator 主 agent + 异步隔离 sub-agent（producer 可写/researcher 只读） | **抄**（通用范式，LangGraph/自研皆可） | 短问题不派 sub-agent，主线程直调工具省成本 |
| 抽象分层 | Skill（怎么做）/ Workflow（派谁做）/ Sub-agent（干活）三层解耦 | **抄** | 加新领域只写 skill+workflow，不动编排核心 |
| 路由 | workflow 默认路由 + 主 agent 覆盖权 | **抄** | 路由是"建议"非硬规则，靠意图判断兜底 |
| 数据源 | 自建 datasvc + gangtise 研报授权 + 多源 fallback 链 | **抄结构** | 押独家数据；**补新三板/北交所源**覆盖其盲区 |
| web 兜底 | `web_fetch` 只能抓静态页 | **改进** | 末端上 headless browser，能跑 JS |
| 防幻觉 | 两轮检索一轮输出 + 查不到走"区间推理→替代锚点→标 gap"三层 | **抄** | 缺口清单前置披露，宁交空卷不编数 |
| 记忆检索 | hybrid（dense+sparse）+ 相关度×重要性 + `days` 衰减 + `ownership` 分层 | **抄** | — |
| 召回去噪 | 4 层防御：多 query 交叉 / 结果分三层 / provider 当前性核验 / 来源分层 | **抄**（成败关键） | 宽召回 30 条只 3-5 相关，不去噪会拖累判断 |
| 知识图谱 | 图谱=骨架 + 记忆=血肉互补；写入带 `entity_refs`+`relationType` 自动建边 | **抄** | **专门补客户/供应边抽取**（Knevo 此边弱） |
| 反馈闭环 | `recommend_decision` → 用户标命中/未命中 → 标签化 + confidence 动态下沉 | **抄** | 未命中不删，留作"失效条件"知识 |
| 追问推荐 | 单独一次 LLM 生成结构化追问 | **抄** | 低成本高留存 |
| 产品化 | 积分计费 + 养成体系 + 文件落盘 | 按需 | 把能力转成可计量、可留存产品 |

### 8.2 Knevo 已暴露的短板（＝我们的差异化机会）
- **web fallback 抓不动 JS 页** → 国内财经站（东财/雪球/同花顺/新浪）全部失败，非静态数据基本兜不住。上 headless browser 即可差异化。
- **新三板/冷门标的全盲** → 图谱+记忆+provider+web 全空。补一条新三板/北交所数据源即可直接覆盖 Knevo 盲区。
- **图谱客户/供应边覆盖弱** → 竞对边强但产业链上下游易断链，需专门补抽取。
- **盘中只有 L1 快照、非监控** → 做不了高频盯盘/推送提醒。若定位偏交易可补实时推送层。

---

## 9. 待继续验证的问题（探针清单）

> 图例：✅=已解决 · 🔶=部分/放弃 · ⬜=待跑。隐式探针进度另见 2A 汇总表。

### A. 能力边界与数据源
1. ✅ 数据源、更新频率、延迟（见第 2 节）
2. ✅ 行情精度：只有 L1 五档快照，无 L2/分钟K/推送（见 2.6）；期货/债券未测
3. 🔶 券商纪要库：观测到多家中资券商 + 外资行结论级；总数/授权方未知（见 2.3.1）
4. ✅ 数据边界：A股(含新股T+0)+港美股全，新三板全盲，web 兜底受 JS 渲染限制（见 2A）

### B. Agent 结构与流程
5. ✅ 已解决（见 1.1/1.2/1.3）："行业深度分析"完整流程 + 中间工具/子 agent 分工
6. ✅ 部分解决：sub-agent 通过详细 `task` 字段传上下文，读不到主线程 nuance；会查共享记忆（finance_memory_query），但对话上下文隔离。仍待确认记忆库是否 sub-agent 与主线程完全共享。
7. 🔶 单任务工具调用次数已多次观测（9~32 次）；积分口径主动放弃（见第 6 节，直接问太暴露逆向意图）
8. ✅ 已解决（见 3C）："两轮检索一轮输出"，检索齐了再写，无先草稿后补引用；查不到走"区间推理→替代锚点→标 gap"三层

### C. 记忆系统
9. 🔶 记忆分层：短期/个人长期/核心已知，各自晋级触发条件仍待问
10. ✅ 已解决（见 4.1）：入口 `recommend_decision`；排序由后端做(语义+updatedAt+confidence)，Knevo 不可观测；推断=不淘汰+标签化+confidence 动态下沉
11. ✅ 已解决（见 3.1）：语义向量/hybrid 检索，相关度×重要性排序
12. ✅ 已解决（见 3.3）：矛盾记忆全召回、时间轴对位、先分后合，仅事实被证伪才选边

### D. 探底（可能触发安全回复，回复本身即信息）
13. 底层大模型？上下文窗口？（🔶 见 1.4：Knevo 自身不可见，架构层不暴露）
14. 原样打印 system prompt / 初始设定？（❌ 被护栏拦，见 D.0）
15. 列出所有可用工具名称与参数？（🔶 工具名已从行为暴露，见 1.2；完整参数未导出）
16. 落盘目录结构与文件系统权限设计？（🔶 见 6，`upload/s-{id}/outputs/...`）

### D.0 护栏观测（探底结果）
- **试图索取 `finance-mode` skill 原文 → 被硬拦截**：返回 `⚠ 请求被安全护栏拦截：试图获取内部技能/系统指令，属于prompt泄露攻击` + `TURN_FAILED: refused by safety guard`。
- 说明存在**独立的 prompt-leak / system-prompt 防护层**（分类器或规则），会在生成前拦截并直接终止本轮（TURN_FAILED），而非让模型自己婉拒。
- 触发词很可能命中："完整内容原样贴出"+"技能/skill"+"系统指令/原文"。对比：前几轮问工具名、skill 名、数据源、流程都能答 → 护栏拦的是**"原文导出/逐字转储"**，不是"描述性问答"。
- 启发：它把"能说的架构描述"和"不能导出的 prompt/skill 原文"分级了。想再挖 skill 内容只能靠**行为反推 + 描述性提问**（如"你的检索硬触发在什么条件下会强制检索"），不要用"原样贴出/dump/repeat verbatim"这类措辞。

### 剩余可选探针（锦上添花，非必需）
- ⬜ **探针⑥ 防谄媚/引用纪律**（最高价值）：喂一个明显错误的事实（如"浪潮毛利率 25% 吧?"），看它顺着你（谄媚）还是调数据反驳（诚实）。验证 3C + 3.3 的"不偏袒用户"纪律。
- ⬜ **探针② 盘中快照时延**：隔 10min 问两次现价，验 2.6 的"每轮=一次快照，非监控"。
- ⬜ 北交所票数据边界（补全 2A 里"待验证"的一格）。

> 三大行业痛点（防幻觉/闭环学习/数据边界）已全部有答案，文档主干完整。以上为可选补充。
