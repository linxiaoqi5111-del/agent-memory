---
title: 金融项目 (finance-workspace-private)
type: project
agent: devin
source: https://github.com/linxiaoqi5111-del/finance-workspace-private (README/AGENTS)
date: 2026-06-28
tags: [project, 金融, 量化, duckdb, 复盘, python]
status: active
related: ["[[knowledge-base-private]]", "[[finance-agent-capability-graph]]"]
---

# 金融项目 — 项目 MOC（Map of Content，内容地图/目录页）

## 概述
- **是什么**：A股**量化复盘 + 研究工具集**。
- **数据来源**：fupanhui.com API（浏览器内 XHR / CDP 代理抓取）、iFinD、AKShare、飞书 Bitable。
- **仓库**：`linxiaoqi5111-del/finance-workspace-private`（private，Python，分支 `main`）

## 目录导览
- `db/` — DuckDB：canonical 库是 `market_feature_store.duckdb`（`market.duckdb` 是早期飞书同步阶段的 legacy 路径，不作为当前数据源，见 [[finance-canonical-data-source-freshness]]）
- `scripts/` — `sync_to_local.py`（飞书→DuckDB）、`detect_turning_points.py`（MA5峰谷+放量信号）、`backfill_sector_marginal.py`（板块边际量回填，CDP代理）、`backtest_sector.py`（板块回测）
- `intelligence/` `market_feature_store/` `research/` `evolution/` `复盘/`
- `skills/` — 各分析模块 SKILL.md（注：ingest 类 skill 已于 2026-06-12 迁至 [[knowledge-base-private]] 的 `skills/`）
- `shared/` → 软链到 `~/.claude/shared`（飞书工具库）
- `CLAUDE.md` / `AGENTS.md` — AI agent 项目指令；`UBIQUITOUS_LANGUAGE.md` — 术语

## 数据流
```
fupanhui API ─(CDP proxy)→ daily-full → DuckDB (market_feature_store)
DuckDB → detect_turning_points.py / backtest_sector.py → 信号+板块边际量策略分析
```

⚠ 复盘数据的飞书 Bitable 写入已废弃，统一走 `daily-full` → DuckDB；飞书仅保留连板晋级/强势股等 skill 的写入。

## 环境要求
- Python 3.9+（duckdb）；Chrome（fupanhui 登录态）；CDP Proxy；飞书凭证 `~/.claude/shared/feishu_config.json`

## 关键约定
- **错误教训唯一沉淀地**：`finance-workspace-private/.claude/lessons_learned.md`（知识库的教训用 `[kb]` 前缀也记到这里）。

## 任务看板
| 任务 | 负责 | 状态 | 备注 |
|---|---|---|---|
| 双时态历史评测 10 日 pilot | devin | done | finance PR #189；扩到 80 日前先人工复核金标准与连续窗口缺口 |
| D8 剧本库扩容：DuckDB 回补 2019~2024 板块逐日历史（pct_chg/diff_ratio/amount），随后写「历史窗口扫描→批量 draft 卡」脚本半自动扩卡（口径与 fact_sector_daily 统一、量能维度补齐；catalyst/环境标签仍走人工/知识库）| 用户回补数据 → devin 写扫描脚本 | 待办 | 2026-07-09 留档；参考 duckdb-backfill skill；卡入库仍走 draft→多源核数→approved 门控 |
| Chat-first Skill Workbench | codex 规划 → devin 实施 | done | PR #190 已 rebase 最新 `main` 并整合 runtime assurance；981 backend / 27 Vitest / 6 Playwright，CI 全绿、mergeable clean；仍为 draft，未合并 |
| Workbench-first 垂直金融 Agent 产品化 | codex 设计 → 用户自用验收 → 再做托管内测 | doing | Phase 0+1 协议层见 #237；题材研究真实 stage 竖切见 [PR #238](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/238)。Deep-Research 运行时加固在 `fix/deep-research-p1b-runtime@1139c952` 完成，尚未合并/部署：市场复盘统一走 grounded 出口，LLM 预算、agent deadline、证据跨轮继承和 QueryLedger single-flight 已补硬约束；Ask 根 watchdog、阶段 trace、共享 RAG deadline 与确定性 `market_forecast` 路由已完成真实事故回放。合并和 canonical 8792 切换仍需用户确认。spec：`docs/superpowers/specs/2026-07-19-deep-research-runtime-hardening-design.md` |
| 题材研究回答分层 | devin | done | [PR #211](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/211)：检索候选、证据裁决与表达层改为 `ThemeResearchSpec → Claim/CompanyAssessment → AnswerSpec → presenter`；稳定币、机器人、算力、低空经济共享配置化协议，CI 全绿，待用户决定合并 |
| Workbench 显式视角切换 | devin | done | [PR #205](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/205)：数据中立 / 单一 KOL / 多视角并列，消息级持久化、用户隔离与独立 BM25 观点召回；CI 全绿，待用户决定合并 |
| delta package 契约强化（manifest v1 / 多维校验 / 安全解压 / 原子回滚 / data-quality CI）| devin | doing | PR #179 待 review/merge，尚未合并；后续单独做历史债务清洗与环境 blueprint |
| 忠实度 / 历史重放验收 | devin → 用户审定 | doing | #197→#201 已合并；a77 固定 runtime `ea86010c` 与 20:05 daily-agent、20:30 PIT、20:45 acceptance LaunchAgent 已上线，等待 7/13–7/17 前向产物及 claim-level Gold 双审；`decision_eligible=false` |

## 交接记录
- 2026-07-19 · codex · 审查并修复 Claude 的 Deep-Research P0/P1-B 分支，当前 tip `fix/deep-research-p1b-runtime@ec1614ba`，未推送/合并/部署：①撤销 market-review 无 marker 散文豁免，复用现有 `DecisionBrief → Grounded Composer → deterministic validator → semantic judge` 作为可信自然语言出口，失败回结构化 AnswerSpec；②LLM 硬预算下沉到每次 `_post_chat*` 网络边界，以锁保护原子 reservation，provider fallback、retry、stream fallback 和并发线程逐尝试消费预算；③agent loop 接收绝对 `ResearchDeadline`，LLM timeout 与内置 KB/Web/News/L3 工具均按 remaining 钳制，零预算零副作用；④跨轮合并保留 `candidate_facts`，`content_hash/source_revision` 完整反序列化并进入 EvidenceAtom provenance；⑤QueryLedger 从全局锁包慢 IO 改为 per-key Future single-flight，同 key 合并、异 key 并行，Web key 纳入 limit/匿名化 proxy variant；⑥AgentEvidence 分离公开来源和 internal locator，citation 投影按稳定语义键去重。验证：相关回归 278 passed，Ruff/py_compile/diff-check 全绿；全量 2066 passed/1 skipped，剩余 13 fail 与修改前相同，均为 external fallback 本地分类及 userspace/vault 宿主环境噪声。可迁移原则：配额要在副作用前“预占”而非事后计数；全链 deadline 应传绝对时刻；请求去重用 per-key single-flight，不能用全局锁包住慢 IO。
- 2026-07-19 · codex · Ask 卡死事故完成闭环，分支更新至 `fix/deep-research-p1b-runtime@1139c952`（未推送/未合并/未部署）：根因是无主体市场展望误入 `general_finance_qa`，通用 closed-loop Wiki RAG 占满根预算且 semantic judge 未共享绝对 deadline。新增受约束 `market_forecast` 路由、Ask 控制面阶段 progress、orchestrator daemon-thread 根 watchdog、RAG 子查询共享 stage deadline、judge deadline 透传；该头部车道跳过通用 agent loop/Wiki RAG，改用市场情景树和量价/扩散反证。8795 临时实例 readiness=`ready`、行情截至 2026-07-17；事故原问题 `run_20260719_233025_107642` 约 1.5 秒完成且 trace 无 agent loop/closed-loop RAG。相关回归 239 + 107 passed；全量中断前 1637 passed/13 既有失败，慢集成单跑 2 passed。可迁移原则：确定性头部意图应在进入昂贵检索前分类；watchdog 必须与阶段 deadline/trace 一起设计，否则只能“超时”不能定位；控制面进度不得携带证据正文或 locator。
- 2026-06-28 · devin · 初次建档（基于 README/仓库结构）
- 2026-06-29 · codex · 新增金融 -> 知识库闭环出口：`agent-daily` 现在基于 `research_queue` 生成 `YYYY-MM-DD-kb-ingest-queue.json`，将概念入库、官方证据、公司边际变化、来源回溯等缺口交给知识库 repo 接收处理；任务包默认 `requires_human_review=true`、`auto_apply=false`。
- 2026-06-29 · codex · 提升问答质量层：新增回答阶段判断与反方审稿上下文，区分“预期交易 / 事实验证 / 兑现分歧”等 A 股阶段；`ask --compose` 会把该上下文注入 LLM 合成提示，`agent` 系统提示也要求主动质疑旧预期、提前交易、一阶/二阶受益、报表弹性和硬事实缺口。
- 2026-06-29 · codex · 新增金融回答质量 rubric：在既有 `agent-eval` 工程门禁之上，增加 `answer-score` 单答案评分器，按本地数据优先、L1-L4 证据分层、盘面阶段、产业推导、反方审稿、结论可用性、双红/流动性等个人方法论贴合度打 100 分；第一版为确定性评分，后续可叠 LLM-as-judge。
- 2026-06-29 · codex · 读取全量盘面体系说明 `https://fupanhui.com/workspace/academy/system`，将“资金推动价格，量能决定周期”、20日量能回归、量能/情绪/结构/行业聚散度四维复盘、六段市场周期、板块成交占比环比、市场→板块→个股资金流动路径沉淀进 `intelligence/foresight_methodology.md`，并同步进 agent/compose 提示与 `answer-score` 个人方法论评分项。
- 2026-06-29 · codex · 优化问答质量上下文：`AnswerQualityContext` 新增 `methodology_checks`，把每日全量复盘数据映射成“市场量能、市场情绪、行业聚散度、板块承接、个股确认”五步市场结构推演路径；`ask` 的分歧反证与后续验证点会显式保留这些检查/缺口，`compose` 提示也注入该路径，避免个股问答直接从公司逻辑跳上涨空间结论。
- 2026-06-29 · codex · 落地问答经验卡片闭环：新增 `users/<id>/experience_cards.jsonl`（gitignore）作为机器学习层，`answer-score --save-card` 可把评分/扣分/修正原则写成卡片；`ask/chat --compose --user <id>` 会检索相关卡片并注入 LLM 合成提示。新增 `docs/learning/README.md` 说明“机器卡片在金融 repo、人类方法论在 agent-memory、事实证据在知识库 repo”的三层边界；同时把潜意识与 checkpoint 回检日志默认 vault 改为显式路径/env/`~/agent-memory`/回退路径。
- 2026-06-29 · codex · 读取策略一/策略四生成逻辑并沉淀为问答方法论：策略一是“主线流动性池内的启动/回流核心确认”，以成交前三行业、双红题材、开根加权强度、新高/多题材命中筛 T1CORE6；策略四是“强者恒强候选池 + 主线 regime 开关”，引擎A 抓加权 Top20 且涨停/重复入榜的大容量动量，引擎B 抓长周期新高且近 8 日多次新高的趋势股。后续问答要把二者当作市场结构语言：先判断主线扩张/拥挤/回流，再判断个股是启动核心、趋势延续、补涨还是高位兑现。
- 2026-06-29 · codex · 读取策略三生成与验证逻辑：策略三是“前期强势核心的分歧回踩/Touch UP 反抽”，不是追高；生成器先限定近 15 个交易日进过单日加权 Top20 的强势池，再用 UP=MA26+0.764×STD26、dev/pdev 判断首次 Touch 或 Touch 后窗口，并把 SELL/RISK 从入选样本中剔除。方法论重点是：先确认核心品种和题材记忆，再看 UP 附近回踩；浅破 -1.5%~0 可容忍，深破<-1.5% 剔除；E1 冰点不等于无脑买，touch 日最多 1/3 底仓，次日首阳或不破 touch 低点再加；共振主线过滤优先，脱离共振的新高/回踩容易是陷阱。
- 2026-06-29 · codex · 补充策略二与策略总原则：策略二是弱市三路径/多周期题材确认，覆盖策略四主升开关关闭时的环境；弱市日优先看跨 1/3/5/10 日周期>=3 的题材且当日未高潮，个股新买入优先题材内低位滞涨补涨腿，弹性腿需 T+1 承接确认，双红穿越核心更多是持仓延续而非新买。用户强调四类策略都是“概率×时间窗口”的方法论，不是静态标签；个股问答必须结合市场环境、板块生命周期、相对强度、位置/筹码、公司事实和反证，输出可审计路径：观察 -> 假设 -> 证据 -> 反证 -> 概率 -> 时间窗口 -> 触发/失效。
- 2026-06-29 · codex · 裕太微问答复盘：用户指出“深挖”不能从题材/策略倒推公司，必须先说清公司本体、主营产品、产业链上下游位置、客户/收入验证、同链相关公司和炒作逻辑生命周期，再把盘面/策略作为辅助视角。已写入 `intelligence/users/linxiaoqi5111/corrections.jsonl`，并新增知识卡 [[finance-stock-deep-dive-first-principles]]；后续个股深挖默认顺序为：公司本体 -> 产业链位置 -> 客户/产品/收入验证 -> 同链对比 -> 炒作逻辑与生命周期 -> 板块主支线与市场环境 -> 反证与后续验证。
- 2026-06-29 · codex · 用户进一步确立问答总原则：所有金融问答必须从第一性原理出发，策略一二三四只是底层市场结构方法论的衍生产物，不代表绝对收益率或正确率；agent 要学习其设计思路并能迁移生成新假设/新策略。个股深挖除目标公司外，还要进入二阶导：沿产业链暴露、客户、瓶颈、替代环节和同链公司发散，像 Serenity Alpha 一样寻找可能更好的投资机会和可验证产业瓶颈。已同步更新 [[finance-stock-deep-dive-first-principles]]。
- 2026-06-29 · codex · 精智达问答纠偏：用户认可产业第一性原理层可到 80 分，但指出缺少每日全量复盘/市场结构推演路径。后续个股深挖必须在“公司本体、产业链瓶颈、客户/订单/收入传导”之后，显式补上大盘阶段、情绪阶段、容量前三行业、双红题材、涨停热度、题材平行关系、高低切/补涨/主线延续、板块发展周期和个股逻辑生命周期；结论要同时回答“产业上为什么成立”和“盘面上现在处于什么生命周期”。已写入 `intelligence/users/linxiaoqi5111/corrections.jsonl` 并更新 [[finance-stock-deep-dive-first-principles]]。
- 2026-06-29 · codex · 精智达市场结构推演路径二次纠偏：用户指出上一版仍像字段 checklist，只能 60 分。新的要求是从策略一二三四抽象市场生命周期推理：大盘层判断初步走强/主升/上升后第一次分歧/反弹/反弹后兑现/探底/冰点修复，并用量能、涨家数、涨跌停、MA5 情绪拐点和市场风格验证；板块层判断双红/涨停热度/容量/平行题材关系下的主线启动、主升扩散、分歧承接、二阶段回流、补涨扩散或高位兑现；个股层判断是否在对应阶段表现出同步强度、相对强度、抗跌回踩、补涨跟随或高位兑现。已更新 [[finance-stock-deep-dive-first-principles]] 与 `corrections.jsonl`。
- 2026-06-29 · codex · 统一 Agent Memory 分层沉淀规则：新增共享 Stop hook `/Users/a77/agent-memory/40_playbooks/check-writeback.sh`，各 repo 的 Claude/Codex `check-writeback.sh` 改为代理到共享策略；四个项目的 `AGENTS.md`/`CLAUDE.md` 与 SessionStart 提示统一为“项目级决策进 `20_projects`、稳定方法论进 `10_knowledge`、单次问答评分/用户纠偏/经验样本进项目学习层”。后续不再把每次对话反馈默认写入项目交接记录，只有升格为项目级规则或稳定方法论时才进入 Agent Memory。
- 2026-06-29 · codex · 修复经验卡片“记了但问答没命中”的问题：`experience_cards.select_relevant_cards()` 新增轻量意图映射，`深挖某股/怎么看某股` 会自动匹配“个股深挖、第一性原理、产业链分析、二阶导、市场结构推演路径”等样板卡；新增单测覆盖 `深挖汇成股份` 命中裕太微/精智达/汇成样板规则。汇成股份这次纠偏本身写入项目学习层 `experience_cards.jsonl` 与 `corrections.jsonl`，不作为 Agent Memory 聊天流水沉淀。
- 2026-06-29 · codex · 将市场结构盘面分析升级为硬性视角：`AnswerQualityContext` 新增 `market_review_checklist`，要求个股深挖/上涨空间回答必须覆盖大盘阶段、量价/20日量能/周均线偏离、MA5情绪与未来5日方向、市场风格、容量前三申万一级与成交占比环比、题材到申万映射、同题材/同行业新高、所属一级涨停结构、双红演变、个股流动性/相对强度/加权涨幅和生命周期身份；缺数据必须显式列缺口。同步新增经验卡片与测试，避免市场结构推演路径退化成可选字段。
- 2026-06-29 · codex · 术语统一：用户要求回答中不再出现“复盘会路径”等措辞，运行时提示、经验卡片、纠偏台账和问答测试统一改为“市场结构推演路径 / 全量盘面推演”；同时把“生命周期身份”明确为“个股逻辑生命周期”，要求输出逻辑从发现、发酵、加速、分歧承接、二阶段回流到兑现退潮中的位置。
- 2026-06-30 · codex · 将 `daily-agent` 底层逻辑并入普通问答入口：`AnswerQualityContext` 新增 daily-agent 推理约束，要求“怎么看/深挖/上涨空间”先判断旧逻辑唤醒/新逻辑候选/数据缺口/噪音，再用生命周期（新出现→旧逻辑唤醒→升温验证→加速定价→高位分歧→衰退观察→证伪退出）、盘面验证强弱、priority/强势股/触发信号变化、历史有效性与研究队列落点组织回答；`llm_refine` 合成提示同步要求市场→板块→个股资金传导、逻辑生命周期、二阶导/产业瓶颈和缺失 daily-agent 数据显式说明，并新增回归测试。
- 2026-06-30 · codex · 将“盘面反向解读”升级为问答硬约束：在保留公司本体/产业链暴露/逻辑生命周期骨架的基础上，普通问答必须从全量盘面数据做正反推导，解释成交额/20日量能回归、涨家数MA5、容量前三、双红、新高、涨停、加权强度分别支持什么、反证什么；尤其要解释强板块弱个股、个股反弹但市场缩量/情绪回落时，市场正在奖励谁、抛弃谁、犹豫谁。已写入 `AnswerQualityContext`、compose 提示、经验卡片意图映射和回归测试。
- 2026-06-30 · codex · 固化晚间卖方与机构胜率发散框架：`AnswerQualityContext` 新增 `sellside_winrate_reasoning`，当证据命中卖方/机构胜率/T+5/T+10/覆盖密度时，要求按“机构胜率 × 覆盖密度 × 证据硬度 × 盘面位置”判断优先发散、只作确认或反向谨慎；经验卡片意图映射新增晚间卖方触发词，并沉淀人类样板 `docs/learning/sellside-winrate-divergence-framework.md` 与知识卡 [[finance-sellside-winrate-divergence-framework]]。
- 2026-06-30 · codex · 建立日度市场前瞻验证闭环：新增项目学习台账 `docs/learning/daily-market-forecast-ledger.md`（**2026-07-02 已迁移至 `docs/learning/forecast-review-ledger/` 按日期拆文件，旧单文件归档 `archive/daily-market-forecast-ledger.legacy.md`**），用于记录“盘前/前瞻研判 -> 收盘验证 -> 经验修正”，每条研判必须写清当时可得信息、数据缺口、可验证假设、强/中/弱路径和收盘后验证字段；单次研判内容留在项目学习层，只有反复有效的稳定方法论再升级进 Agent Memory。
- 2026-06-30 · devin · 在 a77 Mac 上建立 **DuckDB 每日增量自动同步（iCloud → launchd）**。底库 `db/market_feature_store.duckdb`（6-26 整库快照，停在 `2026-06-26`）；另一台 Mac 复盘后把增量包写进 iCloud Drive `~/Library/Mobile Documents/com~apple~CloudDocs/mfs-delta/`（`mfs-delta-YYYY-MM-DD.zip`），iCloud 自动同步到 a77。导入用 `scripts/db_delta_pull.py --sync-dir <mfs-delta> --db <db>`（幂等，按日期旧→新合并未导过的包，状态记 `<db>.delta_applied.json`；内部用 `sys.executable` 子进程调 `db_delta_import.py`，故主进程解释器须自带 duckdb）。**核心坑①（FDA/TCC）**：macOS 自带 `/usr/bin/python3`（CommandLineTools，SIP 保护区）即使在系统设置里授「完全磁盘访问」，TCC 也**钉不住**，读 iCloud 目录恒 `Operation not permitted`；解决=改用 Homebrew `/opt/homebrew/bin/python3`（已装 duckdb 1.5.3）并对它授 FDA，launchd 直接以它为责任进程时 FDA 生效。**核心坑②（责任进程继承）**：远程执行隧道 `exec-a77.industry7view.com` 的 exec-server 跑系统 py3.9 无 FDA，经隧道直跑/或在其下 fork 任何子进程读 iCloud 都会被 TCC 按责任进程拒绝——所以读 iCloud 的活必须交给 **launchd 下的 brew python**，不能走隧道直跑（诊断时也要用 brew-python 的临时 launchd 任务，用 /bin/bash 跑会因 bash 无 FDA 而误报 EPERM）。**核心坑③（iCloud 占位）**：新包同步初期可能是占位文件 `.mfs-delta-….zip.icloud`，名字不匹配 `mfs-delta-*.zip`，脚本先空跑；实体化成真名后文件夹再变一次，靠 WatchPaths 再触发补上（CloudDocs 默认自动下载）。**已部署**：LaunchAgent `~/Library/LaunchAgents/com.mfs.deltapull.plist`（Label `com.mfs.deltapull`，uid 501，`gui/501` 域），`ProgramArguments[0]=/opt/homebrew/bin/python3`，双触发=`WatchPaths`(监听 mfs-delta，一到就补) + `StartCalendarInterval` 每日 9:00 兜底；日志 `/tmp/mfs-deltapull.log` / `.err`；实测空跑 exit=0、输出「同步目录共 0 个增量包」。注意 a77 金融仓当时停在 `feature/answer-quality-critic` 脏工作区，本任务不动该分支：3 个 delta 脚本是从 `origin/main` 取到 `scripts/` 的未跟踪文件。
- 2026-06-30 · codex · 吸收高位主线反证与生命周期分析动作：新增 `AnswerQualityContext.high_position_mainline_reasoning`、经验卡片触发词和人读样板 `docs/learning/high-position-mainline-rebuttal-framework.md`，要求 AI硬件/CPO/PCB/半导体/存储等高位主线回答先看边际预期、流动性、证据硬度、位置约束，再做产业瑕疵审查、事件锚点生命周期和缩量拥挤反证；同步沉淀知识卡 [[finance-high-position-mainline-rebuttal-framework]]。
- 2026-06-30 · codex · 固化个股/题材完整分析路径：新增 `AnswerQualityContext.stock_analysis_entrypoints` 与 `logic_lifecycle_questions`，把“公司本体 -> 产业链暴露 -> 证据层 -> 高位主线反证 -> 大盘流动性 -> 市场风格 -> 行业容量 -> 题材结构 -> 个股相对强度 -> 生命周期四问 -> 二阶导 -> 条件化结论”写入普通问答硬约束；项目学习层新增 `docs/learning/stock-analysis-entrypoint-framework.md`，经验卡片新增“个股与题材完整分析路径”，同步沉淀知识卡 [[finance-stock-analysis-entrypoint-framework]]。
- 2026-06-30 · codex · 落地问答输出前自审：`AnswerQualityContext` 新增 `pre_output_quality_gate` 与 `shadow_user_critic`，`ask --compose` 系统提示要求模型内部先做质检和用户影子反驳，再只输出修正后的最终稿；项目学习层新增 `docs/learning/answer-self-review-framework.md`，经验卡片意图映射支持“质检/反驳/模板化/影子 agent”等触发词，同步沉淀知识卡 [[finance-answer-self-review-framework]]。
- 2026-06-30 · codex · 将问答自审升级为真实二次反驳/重写：`llm_refine.synthesize_messages_with_review()` 先生成初稿，再追加用户影子审稿提示做第二次 LLM 修订；`ask --compose` 默认启用该流程。同时新增本地 DuckDB“市场价值与替代队列数据块 [D1]”，个股问题会自动注入区间涨幅、峰值涨幅、峰后回撤、收益保留率、半衰期代理、题材双红代理、相对强度排名和同题材强势替代队列，减少回答中 CAR/半衰期/二阶导缺失。
- 2026-06-30 · codex · 将“客户证据硬度”和“二阶导研究队列”结构化进 `ask --compose`：新增 D2 客户证据硬度数据块，将客户/订单/量产/导入/送样/收入等证据分为硬证据、候选证据、弱证据和反证缺口；新增 D3 二阶导研究队列数据块，按 P0 盘面已选择强势替代、P1 目标股再升级条件、P2 产业瓶颈补盲组织研究发散，并优先按命中题材排序同题材替代队列。同步更新 [[finance-answer-self-review-framework]]。
- 2026-06-30 · codex · 将飞凯材料问答纠偏固化为“多逻辑高位股补齐四块”：收入结构与利润弹性表、客户证据硬度表、板块生命周期判断、强反证压力；项目学习层新增 `飞凯材料补齐版优秀样板` 经验卡片并扩展 `experience_cards` 意图映射，确保“客户证据/收入结构/板块生命周期/强反证”等自然语言追问能召回该卡。同步更新 [[finance-stock-analysis-entrypoint-framework]]。
- 2026-06-30 · codex · 创建全局 Codex skill `/Users/a77/.codex/skills/finance-stock-deep-dive`，将“深挖个股/上涨空间/质检”封装为本地优先的 A 股个股研究 SOP：必读 finance repo、knowledge-base、agent-memory，默认说明 ask/chat 会走 wiki hybrid RAG，手工读文件不等于触发向量检索；输出覆盖公司本体、收入结构、客户证据硬度表、板块生命周期、市场价值、二阶导、强反证和自评分 rubric。
- 2026-06-30 · codex · 落地问答编排层 P0：新增 `intelligence/services/answer_orchestrator.py` 与 `QuestionPlan`，在 `ask --compose` 前先识别问题类型、回答深度、必需视角、证据计划、质检门槛和缺数据策略；`AskResult` 保存 `question_plan`，`ask` workflow summary 新增 `answer-orchestrator` step，compose prompt 自动注入“问答编排计划”。项目学习层新增 `docs/learning/answer-orchestrator.md`，并沉淀知识卡 [[finance-answer-orchestrator]]。
- 2026-07-01 · codex · 强化次日行情研判编排：`market_forecast` 不再只按全量盘面外推，新增“四源合议”（全量复盘、晚间卖方/机构胜率、隔夜美股/海外映射、晨汇/早间材料）与策略状态映射；分歧时比较策略三主线强势股回流和策略二流动性切换，上涨时区分结构性上涨/普涨并映射策略一主线流动性池与策略四强趋势，高位拥挤时检查核心新高、涨停扩散和共识兑现。同步更新 `docs/learning/daily-market-forecast-ledger.md`（现 `forecast-review-ledger/`）、`docs/learning/answer-orchestrator.md` 与 [[finance-answer-orchestrator]]。
- 2026-07-01 · codex · 明确外盘映射双源口径：复盘会 `https://fupanhui.com/workspace/review/global-market` 对应公开结构化接口 `/api/v1/client/reviews/global-market?trade_date=...`，适合作为可入库、可回测、可与 A 股交易日对齐的外盘底座；但接口可能只返回上一完整外盘日，必须记录 `source_trade_date`。若问题需要“晚间/隔夜美股最新涨跌幅”，web/finance search 是必要实时补丁，不是兜底；后续行情前瞻要同时区分 fupanhui 外盘底座与 web 最新美股口径，避免把昨日外盘误当最新隔夜。
- 2026-07-01 · codex · 将 daily-agent 策略候选并入行情前瞻：`market_forecast` 必须基于市场阶段选择策略一/二/三/四或组合，并输出对应题材/个股、选择理由和次日验证字段；策略一/四用于主线流动性池与强趋势确认，策略三用于前期强势核心分歧回流，策略二用于弱市或核心分歧时的低覆盖硬增量/流动性切换。若未读取真实 daily-agent 候选池，回答必须标注“未读取候选池，仅按方法论推演”。
- 2026-07-01 · codex · 修正行情前瞻硬字段：`market_forecast` 必须复用全量复盘视角，强制判断容量前三申万一级、双红题材、涨停热度、新高集群、开根加权强度和行业发动机。双红定义按 `pct_chg>0 AND diff_ratio>10 AND amount>500`，若方向涨幅强但 `diff_ratio` 为负，必须判为“缩量强修复/存量抱团”，不能误写成低位放量启动；策略选择也要区分真正双红、涨停强、新高强、容量强四类信号。
- 2026-07-01 · codex · 接入 L3 官方证据运行时编排层：新增 `intelligence/services/l3_evidence.py`，在 `ask --compose --l3-lookup` 下先检测个股深挖/新闻影响题是否缺客户、订单、量产、产能、公告、问询函、互动易等硬证据，再按 `FINANCE_L3_CNINFO_CMD` / `FINANCE_L3_SSE_EINTERACT_CMD` 命令模板调用外接公告/互动易 CLI，归一成 `L3EvidenceBundle` 注入回答证据链；未配置/超时/失败只 warning 并要求结论降权。项目学习文档 `docs/learning/l3-evidence-orchestrator.md`，可复用知识卡 [[finance-l3-evidence-orchestrator]]。
- 2026-07-01 · codex · L3 evidence 工具入口从“仅可配置”升级为默认组合命令：`python -m disclosure_lookup.cli company {company} --days 30 --source cninfo,sse_einteract`，并把默认超时提高到 480 秒，覆盖 `sse_einteract` 首跑构建 `sse_uids.json`（实测 411.5 秒，2304 条映射，`688323 -> 201868`）的静默等待；缓存后约 24 秒。仍可用 `FINANCE_L3_COMPANY_CMD` 覆盖，或清空后回落到单源 `FINANCE_L3_CNINFO_CMD` / `FINANCE_L3_SSE_EINTERACT_CMD`。
- 2026-07-01 · codex · 新增“叙事回答编排器”约束，解决个股深挖按模板填空的问题：`llm_refine` 的 compose 系统提示现在要求先内部写核心矛盾句（公司真实业务/市场交易预期/最大证据缺口或反证），所有视角必须服务核心矛盾，禁止按公司本体、盘面、二阶导、反证逐项填空；`answer_quality` 新增 `narrative_composer`，二次反驳检查是否抓住核心矛盾、每段是否回答“这个事实改变了什么判断”。项目文档 `docs/learning/narrative-answer-composer.md`，知识卡 [[finance-narrative-answer-composer]]。
- 2026-07-01 · codex · 统一当前问答/个股深挖/复盘前瞻的 DuckDB 标准数据源：canonical path 为 `db/market_feature_store.duckdb`，`db/market.duckdb` 标记为早期飞书同步阶段 legacy 路径；深挖 skill 新增 freshness probe 要求，使用盘面前先查 `fact_market_daily/fact_stock_daily/fact_sector_daily/fact_sector_stock_daily/fact_mainline_sector_daily` 的 `max(trade_date)` 并在涉及“最新盘面”时说明口径。仓库文档新增 `docs/learning/current-duckdb-source.md`，避免 agent 被旧文档路径带偏。
- 2026-07-01 · codex · 将汇成股份升级为 95 分个股深挖样板：新增 `docs/learning/stock-deep-dive-95-huicheng-template.md`，同时沉淀“输出样板 + 生成路径”。样板要求先验鲜 canonical DuckDB，再跑 wiki hybrid RAG、L3 官方证据边界，抽市场状态/题材状态/个股市场价值/同题材替代队列/申万行业映射五类数据块；汇成案例基于 2026-06-30 最新盘面重判为“旧逻辑唤醒 -> 二次加速定价 -> 高位分歧观察”，核心反证是半导体/先进封装强而个股转弱。经验卡片追加 `汇成股份 95 分成熟样板`，全局 `finance-stock-deep-dive` skill 已指向该生成路径。
- 2026-07-01 · codex · L3 官方证据运行时接入补齐“缓存 + 按需查询”工程口径：`l3_evidence` 支持 `FINANCE_L3_PYTHONPATH` / `FINANCE_L3_CWD`，可从 finance repo 调用 finhot worktree 内未安装的 `disclosure_lookup`；新增短 TTL stdout 缓存（`FINANCE_L3_CACHE_DIR` / `FINANCE_L3_CACHE_TTL_SECONDS`），只缓存成功查询结果，不缓存失败。推荐同步问答默认走 `cninfo` 快路径，`sse_einteract` 作为需要互动易/董秘问答时的慢路径或预热后路径；高价值硬事实再升级进知识库，普通短期公告/互动问答不全量向量化。
- 2026-07-01 · codex · 新增 L3 ingest P0 编排入口：`python3 -m intelligence.cli l3-ingest company <公司>` 调用运行时公告/互动易工具，逐条解析返回结果，并按规则门控生成 `raw_items / candidates / rejected` payload；P0 只生成候选，不写知识库。候选分类覆盖订单/合同、客户验证、产能/项目进展、财务/出货口径、监管/风险边界；可转债赎回、例行治理、低信号公告默认 rejected，避免公告噪音污染 RAG。
- 2026-07-01 · codex · 补齐 L3 ingest P1 `apply`：`python3 -m intelligence.cli l3-ingest apply <payload.json>` 默认 dry-run，显式 `--apply` 才把候选沉淀到 knowledge-base wiki；当前写入边界为 `wiki/sources/<公司>_L3官方证据_<日期>.md` + `wiki/entities/<公司>.md` 的 `## L3 官方证据` 摘要段，形成可被全文/RAG召回的 L3 证据资产。暂不直接写 `relations/evidence_index`，因为那会影响 Theme Radar 排序和图谱权重；后续应走人工复核或 knowledge-base `disclosure-archive` reviewed apply。`--reviewed` 仅用于把 source note 标记为 `review_required=false`。

- 2026-07-03 · devin · 全仓审计发现测试副作用 bug（PR #124 待合并）：`test_l3_evidence.py` 两个 `from_env` 用例没隔离结果缓存，跑一遍测试会把 mock stdout 写进真实 `~/.cache/finance-l3`（15 分钟内真实 ask --l3-lookup 会命中假数据），且二次跑套件因缓存命中必挂 12 例。修法=测试环境里加 `FINANCE_L3_CACHE_TTL_SECONDS=0`。**可复用教训**：单测走 `from_env`/默认配置路径时要警惕磁盘缓存/日志/全局目录副作用，显式关掉或指向 tmp。
- 2026-06-30 · devin · 新增跨机增量同步基础设施（替代每日传 759M 整库快照）：scripts/db_delta_export.py 按 trade_date 把当日 13 张 fact 表增量导成 parquet+manifest 的 ~1MB zip；db_delta_import.py 在目标库按日期 delete+insert 幂等合并（INSERT BY NAME，整体事务）；db_delta_pull.py 扫描同步目录、按日期从旧到新自动导入未应用包（状态游标 <db>.delta_applied.json）。通道用「自动同步文件夹」抽象：当前走 iCloud Drive（两台 Mac 同 Apple ID，目录 ~/Library/Mobile Documents/com~apple~CloudDocs/mfs-delta/），换坚果云/Dropbox/对象存储(rclone 挂载)只改 --sync-dir/--out 路径、脚本不变。生产端复盘收尾用 export --out 直写 iCloud，消费端用 launchd 每天定时 db_delta_pull 自动导入。首次在新机器仍需整库快照建底库（schema 必须先存在）。已合并 PR #100/#101，skill market-overview 收尾步骤已记录。可复用知识点：全量建底 + 每日分区增量 + 幂等 delete-insert + 落地目录状态游标，是离线数仓/特征平台/CDC 消费端的通用同步骨架。
- 2026-06-30 · devin · a77 同步**已跑通到 2026-06-30**（26/29/30 三包导入，max trade_date=2026-06-30，367 行），并修了一个会反复发作的坑：iCloud 新包初期是 **dataless 占位**（文件名是真名 `mfs-delta-*.zip` 但内容没下载），直接 `zipfile.ZipFile` 会报 `OSError [Errno 11] Resource deadlock avoided` → `BadZipFile`，而经隧道/无 FDA 的 `brctl download` 又被 TCC 拒（`NSCocoaErrorDomain 257`）。**解法（已部署）**：在 db_delta_pull 前加一层 `/Users/a77/.mfs/deltapull_wrapper.py`——它在 launchd 的 **brew-python 责任域内** subprocess 调 `/usr/bin/brctl download` 逐个强制下载（子进程继承 brew python 的 FDA，故有权），轮询 `st_blocks*512 >= st_size` 确认实体化（≤5min），再调 db_delta_pull。`com.mfs.deltapull` 的 `ProgramArguments` 已指向该 wrapper，仍保留 `WatchPaths`(一到就补)+每日 9:00 双触发；实测 brctl rc=0、materialized=True、幂等空跑 exit=0。**通用教训**：消费 iCloud/网盘按需下载文件前必须先强制 materialize 并等落盘，否则按名存在 ≠ 内容可读；且强制下载工具要跑在持有 FDA 的责任进程下。
- 2026-07-01 · codex · PR #102 补齐复盘会“每日主线题材 -> 核心板块 -> 板块周期状态”语义桥：新增 `fact_mainline_sector_daily`（L4_market_signal，主键 `trade_date/theme_code/sector_ts_code`），同步脚本 `sync_fupanhui_mainline_sector_daily.py`、CLI `sync-mainline-sector-daily`、日更挂载和只读验证脚本；2026-06-30 验证写入 3 themes / 10 sectors，重复同步 10->10 幂等，AI算力/半导体/有色金属抽查 cycle_status 正常。字段保留 `cycle_level/status`、`startup_date_*`、新高/临近突破、强度/资金等，不强制 `dim_sector` 外键以保留复盘会自有 `*.FP` 板块（如 MLCC）。跨 Mac 增量同步无需改 export/import，因为新表是 `fact_` 且含 `trade_date` 会被自动发现；但目标 Mac 必须先更新代码并 `init_db` 建表，否则导入端会因目标库缺表跳过该表。注意 `mainline-sectors.amount` 保留原始 API 单位，后续查询/评分不要直接和 `fact_sector_daily.amount` 混比。
- 2026-07-01 · codex · 将 `fact_mainline_sector_daily` 接入普通问答 compose 链路为 D4 数据块：`ask --compose` 会从本地 DuckDB 生成“主线题材结构数据块 [D4]”，内容包括最新主线日期、近 20 日主线持续性、核心板块、`cycle_status/cycle_level`、启动日、新高/临近突破、涨停数、板块涨幅、`diff_ratio` 与成交额，并明确区分“真正双红/增量启动”和“缩量强修复/存量抱团”。`llm_refine` 系统提示要求使用 D4 判断主线连续性、启动/顺势/分歧/消亡；`answer_orchestrator` 检索计划增加 D4/主线结构。验证：D4 helper、compose prompt 注入、系统提示、ask chat 回归与实际 `ask 'AI算力怎么看' --compose --no-modules --no-wiki-rag` 均通过；无 LLM key 时仍降级模板，但引用来源会出现 `[D4]`。D4 仍是 L4_market_signal，不写 wiki/entity 正文。
- 2026-07-02 · codex · 新增复盘正式生成前的 `forecast_preflight` 查漏门：`market_forecast`/`ask --compose` 会先读取 daily-agent 的 `research_queue`，检查旧逻辑唤醒、新逻辑候选、DeepDive/IMA 缺口与 L3 官方证据缺口；若存在 `today_do_ima` 或 `today_find_official_evidence`，正式复盘暂停，先输出需要用户补 DeepDive/官方证据并 ingest 的清单，补齐后再生成正式版。该层做成独立 service 而非 prompt-only，方便编排层、daily-agent 与 HTML 台账共用同一 readiness 判断；缺 daily-agent 单独标记为 `missing_daily_agent`，草稿仍可生成但必须带缺口提示。验证：`intelligence.tests.test_forecast_preflight`、`intelligence.tests.test_answer_orchestrator`、`tests.test_research_queue`、`tests.test_daily_agent` 共 19 项通过。
- 2026-07-02 · codex · 新增跨仓金融 Agent 能力图谱 [[finance-agent-capability-graph]]，把 finance repo 的 CLI/问答编排/复盘验证/学习闭环，与 knowledge-base repo 的 ingest/RAG/Theme Radar/relations 画成可维护的 Mermaid 总览图；后续新增稳定入口、服务模块、知识库管线或学习节点时，应同步更新该图和节点清单。
- 2026-07-02 · claude · 统一复盘推演台账结构：废弃单文件 `docs/learning/daily-market-forecast-ledger.md`（已归档 `docs/learning/forecast-review-ledger/archive/daily-market-forecast-ledger.legacy.md`），统一到 `docs/learning/forecast-review-ledger/`（按日期拆文件 + `index.md` 导航 + 每日 HTML）。每份 `YYYY-MM-DD.md` = Codex 答卷(§1-6) + Claude 答卷(§6.5，双盲) + T+1 回填(§7) + 用户批注(§8)；双盲纪律=同源数据、独立答卷、互不可见，考生不做对比/批注/裁决（归用户 + 统一指标）。并排可视化 `复盘/daily/YYYY-MM-DD/dual-blind-compare-*.html`。上方旧路径引用保留历史事实并已加迁移注记。
- 2026-07-02 · devin · **RAG/Hybrid 检索可观测层落地（PR #106，等用户确认合并）**：`intelligence/services/kb_rag.py` 新增 `RetrievalTelemetry` dataclass，`retrieve()` 全路径填充——检索方式（hybrid=BM25+BGE-m3稠密向量+RRF / rerank）、索引类型（结构版 `.rag_index` / 全文版 `.rag_index_full` / custom）、k、filters、命中数、邻居扩展命中、分数 max/mean/min、子进程耗时 ms、索引降级标记（全文缺失回退结构版由藏在 warning 字符串升级为 `degraded` 布尔），状态机 ok/empty/skipped/error/timeout；`ask.py` 的 `AskResult.wiki_rag_telemetry` 贯穿到回答（meta 行 `W检索=hybrid/structured·命中N·ok` + 证据链「图谱·语义召回」小节首行完整摘要）。检索行为逐字节不变，`test_kb_rag.py` 新增 3 用例。**可复用知识点**：检索可观测层=「检索计划→实际召回源→召回质量」显式留痕，是离线评估(recall@k/nDCG)与 A/B 调参前提；「静默降级必须显式留痕」是通用可靠性模式。**重要发现/风险**：capability graph 里的 answer_orchestrator.py / forecast_preflight.py / l3_evidence.py 等只存在于 a77 Mac 本地 `data-source/mainline-sector-daily` 脏工作区，**从未 push 到 origin**；本 PR 基于 origin/main，Mac 那批工作日后 push 时 ask.py 可能与本 PR 冲突，建议尽快把 Mac 工作区提交推送。
- 2026-07-02 · codex · 新增项目学习层架构脑暴 `docs/learning/finance-agent-skill-expansion-brainstorm.md`：将金融 Agent 下一阶段聚焦为“顶级投研助理 + 可解释 RAG 系统样板”，暂不推进自动 apply/自动入库/自动交易，关键金融事实保留人工复核；梳理市场痛点（信息增量识别、证据/推演/盘面信号分层、题材标签绑架、生命周期判断、市场结构影响交易价值、RAG 命中不等于可信）和可扩展技能池（证据分层审计、个股研究路径编排、RAG 命中质量解释、反证与证伪点、题材生命周期、卖方观点发散、公司证据缺口雷达、市场结构状态机、事件冲击传导、估值与预期差、组合风险暴露、信息源可信度评分）。建议下一份正式 spec 先做“个股深挖研究编排器：QuestionPlan -> EvidenceAudit -> StockResearchBrief -> 自然回答”，兼顾投研质量和 RAG 可解释性。
- 2026-07-02 · devin · 按脑暴手册（docs/learning/finance-agent-skill-expansion-brainstorm.md）落地 P0 四技能，PR #107（分支 data-source/mainline-sector-daily → main，待用户确认合并）：新模块 intelligence/services/research_brief.py 提供 ①证据分层审计（逐条 L1/L2/L3/L4/E 归层 + verdict 裁定档位「事实验证/预期交易/情绪脉冲/证据不足」，L3 需官方来源词+硬动作词且无投机词）②RAG 命中质量遥测（检索模式/索引/来源分布/分数/邻居扩展/降级/L3 覆盖，即 RAG observability）③反证与证伪计划（最强反证候选+降级触发+T+1/3/5 验证排期）④StockResearchBrief 八步编排（公司本体→题材暴露→证据硬度→市场价值→同链对比→生命周期→反证→条件化结论，缺视角显式写缺口）。ask 链路接入：新增输出 section【检索可观测】，compose 时四个 prompt block 注入约束回答不得越级写结论，CLI ask --brief-json 导出 JSON。全部确定性纯函数、守三红线（不自动入库/不改知识库/不出交易指令）。19 个新单测，全量 495 例通过。可复用知识点：证据分层审计+检索遥测是高风险 RAG（医疗/法律）通用样板，面试可讲 evidence-grounded generation + RAG observability。
- 2026-07-02 · devin · 手册第二/三阶段继续推进（同 PR #107）：P1 三模块 market_structure.py（六阶段状态机：主升扩散/顺势分歧/高位兑现/高低切/缩量轮动/冰点修复，个股与题材共享的公共依赖）、theme_lifecycle.py（八阶段生命周期诊断，证伪/衰退只认证据正文防方法论词误触发，'跌停'→'跌停潮'防全市场计数误判）、sellside_divergence.py（卖方观点三分类：优先发散/只作确认/反向谨慎，复用 P0 L3 判定）；第三阶段最小版 retrieval_audit.py 检索审计台账（ask --audit-ledger 逐次落 query→命中→质量 JSONL，失败样本自动打标 empty_retrieval/wiki_degraded/insufficient_evidence/no_l3_coverage，summarize_ledger 出 L3 覆盖率/失败率——recall@k 评估底座）。ask 链路：每次回答判定市场结构阶段入分歧反证，题材类问题输出生命周期阶段。新增 25 单测，全量 520 通过。
- 2026-07-02 · devin · 手册收尾（同 PR #107）：P1-7 evidence_gap_radar.py（六维缺口雷达：baseline/收入结构/客户/L3官方/同链/市场价值，只出缺口+候选任务不写库）、P2-12 source_credibility.py（来源分级 0.95公告→0.2社媒，rerank_by_credibility 轻量重排）、P2-9 event_transmission.py（事件冲击六步骨架，无 L3 强制按假设）、P2-10 valuation_gap.py（估值四问框架版，不给目标价）；ask 接入：news_impact→传导骨架、个股深挖→缺口雷达+估值四问。P2-11 组合风险暂缓（无持仓场景）。手册 12 项技能已落地 11 项，新增 11 单测，全量 531 通过。
- 2026-07-02 · devin · Review 层输出闸门收口（同 PR #107）：output_review.py 六项确定性检查（新鲜度/分层/反证/缺口/可验证假设/弱证据硬写），新增【输出质检】section；与 answer_quality 的 prompt 引导形成'柔性引导+程序化 gate'双层质检。至此手册§三四层架构（Intent/Retrieval/Reasoning/Review）全部落地；技能 12 项落地 11 项（P2-11 组合风险等持仓场景）。全量 539 单测通过。注意：分支上出现来自 Mac 侧的 style 提交（b0ec403 清理行尾空格），已 rebase 合入。
- 2026-07-02 · devin · D1-D4 per-block 可观测补齐（同 PR #107）：DBlockStat 记 attempted/generated/行数/未命中原因，进【检索可观测】与审计台账 d_blocks 字段。手册§三 Retrieval 层可观测全部达标。全量 543 单测通过。
- 2026-07-02 · devin · PR #107 合并前审查记录（另一 IDE 复核）：两个 caveat——① test_agent.py:424 与 agent.py:290 行为不一致（market_db_path 未传时会自动探测默认 duckdb，本机有 DB 则测试挂；非 #107 回归，main 同样失败；临时解法 MARKET_FEATURE_STORE_DB=/nonexistent 隔离，后续需统一'自动启用 vs opt-in'）② registry-check 本机失败源自 knowledge-base-private 仓本地脏状态，与金融仓无关。其余全过：mergeable、无冲突、无风险文件、GitHub CI 绿。
- 2026-07-02 · devin · 按用户确认'先修再合'：MarketLiveToolTests 加环境隔离（MARKET_FEATURE_STORE_DB 指向不存在路径，修掉'机器有默认 duckdb 则 opt-in 断言被击穿'的环境依赖；行为语义 自动探测 vs opt-in 的统一留待后续单独决策），复现验证：造假默认库→旧测试挂/新测试过。随后 PR #107 已合并 main（mergeable=True，GitHub CI 绿）。
- 2026-07-02 · devin · 输出稳定性问题（深挖/复盘先验不按满分模版输出）修复：把 agent-memory 三篇方法论（95分路径/自审框架/编排层）固化成金融仓 `skills/stock-deep-dive/`（输出契约 + dispatcher 触发词 + `answer_lint.py` 维度覆盖率 exit-code 门，两轮不过标低置信），PR #109 待确认。根因：模版是知识笔记、离生成时刻太远且无机械校验。同步在 agent-memory 补 `scripts/vault_lint.py`（frontmatter/死链/inbox老化）与 `scripts/graph_audit.py`（能力图谱节点路径防漂移），能力图谱补 answer_lint 质检门节点。
- 2026-07-02 · devin · 编排层优化三件套落地并合并：① PR #112 few-shot 样板锚——`skills/stock-deep-dive/exemplars/` 下 `deep-dive-*/forecast-*.md` 按问题类型在 `ask --compose` 时自动注入 synthesis prompt（≤3 篇/6000 字符，带"只学结构、严禁照抄结论"硬约束），首篇样板改写自汇成股份 95 分案例；沉淀新样板=往目录加文件零代码。② 同 PR：L3 结果缓存默认开启（`~/.cache/finance-l3`、TTL 900s，`FINANCE_L3_CACHE_TTL_SECONDS=0` 可关），解决同会话重复拉 SSE uid 映射超时。③ PR #113 researcher-valuation 技能包 P1 契约层——`QUESTION_VALUATION` 路由（拍估值/估值带/贵不贵/隐含预期，深挖强触发词优先级更高）+ SKILL.md 五段契约（估值现状分位→可比估值带→隐含增长率反推→三情景表带可验证条件→证据审计）+ `valuation_lint.py`（6 维覆盖率 + "目标价 X 元"单点红线正则）。P2（valuation_estimate.py 确定性计算）/P3（隐含反推+情景表）待推进，spec 已确认。可复用知识点：few-shot 锚 + 契约 lint 是"文风"与"结构"分工互补的稳定性组合。
- 2026-07-02 · devin · researcher-valuation P2 落地（PR #114 待确认）：`intelligence/services/valuation_estimate.py` 确定性计算——ValuationSnapshot（PE_TTM/PB/总市值）、peer_band（min/中位/max 只给区间）、percentile_rank 横截面分位、build_valuation_block 生成 D5 估值数据块；取数改走**东财免费快照接口 push2**（iFinD 已失效，与知识库仓 company-baseline-ingest 的 AkShare/东财路线一致），`FINANCE_VALUATION_FETCH=0` 可关。ask 接线：QUESTION_VALUATION + --compose 时注入 D5（目标 DuckDB/问句解析，可比取同板块成交额前排≤4家）+ 估值四问缺口 + exemplar 前缀 `valuation-`。历史 PE 分位数据源不可得，块内显式缺口（留 P3）。实测 600519 PE 18.18/PB 6.38/市值 1.5 万亿正常。12 新测试，全量 579 通过，registry-check 绿。同日：知识库 8 个 ready PR（#140/158/159/160/161/163/166/170）全部解冲突合并——relations 大 JSON 语义三方并集 + md 冲突块行级并集，合并后 integrity 0 错误、meta 已 repair。
- 2026-07-02 · devin · 闭环系统优化（源自 [[agent-system-closed-loop-first-principles]] 五断点分析）金融侧三个 PR 待合并：① PR #115 评估默认化——`ask/chat --compose` 后默认后台跑确定性 `finance_answer_rubric` 打分（零 LLM 成本），分数落 `users/<id>/answer_scores.jsonl`，低分（<70）自动生成经验卡候选进 `experience_cards.pending.jsonl` 复核队列，`--no-score` 逃生口；② PR #116 闭环健康度报告——`scripts/loop_health_report.py` 聚合回答均分/评分次数/经验卡新增与命中/回补队列状态，产出周报 markdown，可挂 daily-ops 晚间流程；③ PR #117 跨仓回执消费端——新 `kb-queue-status` 子命令读知识库 `receipt.json` 看缺口已补/待处理，`daily_agent` 出 kb_ingest_queue 时自动过滤已 ingested 主题（`summary.resolved_themes_skipped` 留痕），配对知识库仓 PR #192。经验卡注入本已默认（answer_query 默认加载相关卡），断点2 无需改动。
- 2026-07-02 · devin · 双盲答卷工装（PR #119 待合并）：给 Claude/Codex 双盲前瞻流程补三块——`scripts/dual_blind_forecast.py` 的 `manifest`（答卷前冻结输入：DuckDB 截止日+材料 sha256+知识库 commit+manifest_sha，防止两考生输入不一致污染比较）、`validate`（机器可读答卷 `<date>.answer.<agent>.json` 必填字段+可证伪 falsify+manifest_sha 一致性校验）、`aggregate`（跨期按 agent 聚合标的池 T+1/T+3 均值/跑赢/阈值命中率——单期噪声大，系统性偏差看聚合，确认后才沉淀经验卡/rubric 检查项）。模板与台账 index 已更新为强制工装流程。
- 2026-07-02 · devin · 台账统一落地（PR #122 待合并，配对知识库仓 PR #196）：按「台账统一设计方案」§4——`dual_blind_forecast.py` 新增 `verdict`（盘后验证唯一机器可读落点 `<date>.verdict.json`，逐假设 hit/miss/partial/unverifiable，id 须落在答卷假设集内：可选 `hypotheses[].id`，未声明时从 thresholds/picks 派生 market/direction/falsify/target:<code>；hit/miss/partial 必须填 actual）+ `index`（扫描 manifest/answer/verdict 重建 index.md 的 AUTO 标记区状态总表，人写导航区不动）；新增 `docs/learning/ledger-map.md` 台账地图（全部台账 canonical 路径×格式×唯一写入者×是否提交 + 去重复边界：复盘假设不再登记 foresight checkpoints、晨汇 T1/T2/T3 兑现并入 verdict、卖方材料第一落点 = 知识库仓 wiki/raw/sellside/）。注意：registry-check CI 在 main 上既有失败（需 main 跑 build_registry.py scan 提交），与本 PR 无关。
- 2026-07-03 · codex · 按新版台账流程更新卖方覆盖密度交叉样本：不再手写进 `forecast-review-ledger/YYYY-MM-DD.md` 的未来小节，而是生成 `2026-07-03.manifest.json` + `2026-07-03.answer.codex.json`，用 `dual_blind_forecast.py validate/index` 校验并重建索引；同时修复 manifest 读取 DuckDB 截止日只查 legacy `daily_market` 的问题，改为优先 `fact_market_daily`、回退 `daily_market`，并补单测锁定当前 canonical 表名。
- 2026-07-03 · codex · 合并 PR120/PR122 两套复盘评判机制：`dual_blind_forecast.py` 现在同时支持 PR120 的答卷级 `source=duckdb|briefing|sellside` 分账与 `recheck` 自动回填数值类收益指标，以及 PR122 的假设级 `flow_type/window`、`checks/falsifiers`、`verdict.json` 逐假设裁定；`aggregate` 保留按 agent/source 的标的池统计，并新增按 `flow_type` 的 hit/miss/partial 聚合。2026-07-03 Codex 样例已迁到 `flow_entries` + hypothesis 检测/证伪字段。
- 2026-07-03 · codex · Perspective Lab / 视角实验室设计落档（分支 `feat/perspective-lab`，提交 `297e306`）：用户选择 C 方案“多角色辩论台”，spec 写入 `docs/superpowers/specs/2026-07-03-perspective-lab-design.md`。关键决策：事实层（DuckDB/知识库/公告研报证据）与认知层（博主/价值投资/趋势交易/用户框架）分离；P0 先做结构化角色画像 + 多角色合议 + 本地私有落档，P1 接 `ask` 与文章 RAG，P2 接 checkpoint/forecast ledger 做角色胜率和盲区评估；博主文章与角色画像默认进入 `intelligence/users/<user>/perspectives/`，不写知识库事实层。
- 2026-07-05 · codex · Perspective Lab P1 已接入日度复盘流程（PR #128，分支 `feat/perspective-lab`）：新增 `intelligence/services/framework_interpretation.py`，`daily-review` 可按用户框架输出判断并自动落 T+1/T+3 checkpoint，checkpoint 带 `framework_version`，偏差可回灌 `corrections`；信号命中从整句匹配改为片段级匹配，报告和判断中保留命中依据；`AGENTS.md` 补充了框架纠偏强制规则。该能力仍属于认知层/用户框架层，不改变事实层知识库写入边界。
- 2026-07-05 · codex · FinHot 与金融 Agent 营销链路 spec 落档并推送（分支 `codex/docs/marketing-agent-chain-spec`，提交 `a88477a`）：spec 写入 `docs/superpowers/specs/2026-07-05-marketing-agent-chain-design.md`。关键决策：主 spec 留在 finance repo，video-use 作为短视频下游工具而非营销主控仓；P0 先做 `docs/marketing/` 素材事实库、claims/personas/content brief/performance JSONL，人工审核发布；P1 再接 Hybrid 检索（BM25 + 向量 + rerank），P2 接效果复盘闭环；营销表达坚持证据先于文案、研究员定位、不承诺收益、不自动投放。
- 2026-07-05 · devin · 营销链路 spec 定位纠偏（提交 `5c5283a`，同分支 `codex/docs/marketing-agent-chain-spec`）：用户明确「以介绍产品为主，不以教学为主」。spec 更新：一句话目标加"以介绍产品为主线"、新增原则 2.4「产品介绍为主线，教学为辅线（每批教学向占比≤20% 且须落脚产品能力）」、目标人群表加优先级列（主观交易者/财经内容创作者=主线，AI 工具爱好者=辅线，删去"非科班转 AI"人群）、content_types 换成 feature_showcase/use_case_story、第 18 节首批清单全部改写为产品 demo/功能全景/使用场景类。已落 correction 至 `intelligence/users/<user>/corrections.jsonl`。
- 2026-07-05 · devin · 营销链路 P0 落地（同分支 `codex/docs/marketing-agent-chain-spec`）：执行清单 `docs/superpowers/plans/2026-07-05-marketing-agent-chain-p0.md`（全勾）；数据契约 `docs/marketing/`（products 2 / features 8 / claims 7 / personas 3，主线=主观交易者+财经创作者，辅线=AI 工具爱好者）；首批 brief `content-briefs/2026-07-05-launch-batch.yaml`；首批内容 `generated/2026-07-05-launch-batch.md`（短帖 10 + 长文大纲 3 + 短视频脚本 2，教学向 13%≤20%，含 Review Gate 自查，未发布待人工审核）；效果台账 `performance/marketing-performance.jsonl`（模板行）；校验脚本 `scripts/validate_marketing_contracts.py`（契约引用完整性 + 禁用表达扫描 + 台账字段，已跑通）。发布仍需人工过 review_required claims（SP-02/04/09/10、SV-02）。

- 2026-07-05 · devin · 营销链路 P0 收尾并开 PR #129（codex/docs/marketing-agent-chain-spec → main，待用户确认合并）：追加只读生成上下文 CLI `scripts/marketing_generation_context.py --brief <id>`（按 brief 组装产品边界/personas/claims+证据/约束的生成上下文包，spec §9.1 P0 显式引用策略的工程落点，不写盘不调 LLM）+ `tests/test_marketing_contracts.py`（契约校验/上下文构建/未知 brief 报错，3 例通过）。注意：本仓 Devin GitHub App 无权限（PAT 接入），builtin PR 工具不可用，PR 经 REST API 创建。

- 2026-07-05 · devin · 首条可发布营销视频成片（SV-01 FinHot 60s demo）：改用 vidio 仓已装的 HyperFrames（vibe motion）技能链——HTML+GSAP 即视频，edge-tts（zh-CN-YunxiNeural）配音，`npx hyperframes lint/inspect/render` 出片。成片 1080x1920 · 46.2s · h264+aac，5 场景（Hook/多源聚合/官方披露查新/本地优先/CTA），素材用 finhot repo 真实截图（docs/screenshots/timeline|entry-detail.png）+ logo，全程带「仅产品演示 · 不构成投资建议」角标。工程源码入 vidio 分支 `feat/finhot-demo-60s`（hyperframes-projects/finhot-demo-60s/），场景配置入 finance `docs/marketing/video/2026-07-05-finhot-demo-60s.yaml`（同 PR #129 分支）；MP4 不入库，走会话附件交付。环境注意：hyperframes 需 Node≥22 + chrome-headless-shell（HYPERFRAMES_BROWSER_PATH）。
- 2026-07-06 · codex · 在当前分支重新修复双盲答卷 manifest 截止日门禁：`scripts/dual_blind_forecast.py` 的 `_duckdb_max_trade_date()` 现在优先查 canonical `fact_market_daily`，再回退 legacy `daily_market`，避免冻结清单 `duckdb_cutoff` 因旧表缺失被写空；`intelligence/tests/test_dual_blind_forecast.py` 补回归测试。按模板生成 `docs/learning/forecast-review-ledger/2026-07-06.manifest.json` 与 `2026-07-06.answer.codex.json`，`validate` 通过；本日未找到复盘材料，答卷 §0 已显式标缺口。
- 2026-07-06 · claude · DuckDB 表结构两层状态澄清与 CLAUDE.md 更新（worktree 分支 `worktree-docs-duckdb-table-map` 基于 origin/main，待用户确认合并）：核实"两层并存"只在代码层成立，数据库层只有一个可用库——主库 `db/market_feature_store.duckdb`（星型模型 29 张 fact_/dim_/config_/feature_ 表，唯一数据源，daily-full CLI 写入）；旧库 `db/market.duckdb` 文件已移除，旧表名 `advancers/daily_market/sector_marginal/stocks` 在主库既非表也非视图、不存在。旧脚本 `detect_turning_points.py`/`backtest_sector.py`（连旧库 market.duckdb）、`sync_to_local.py`/`backfill_sector_marginal.py`/`render_daily_review_*.py`（写旧表）仍写死 legacy 路径，**当前跑会报 Catalog Error 或连不上，属 broken 待迁移**。CLAUDE.md 本地数据库章节（原表格误把旧表当现役）改为：星型表清单（fact_market_daily / fact_sector_daily 双红主表 / fact_theme_limit_heat_daily 涨停热度主表 等）+ 严格双红定义（pct_chg>0 且 diff_ratio>10 且 amount>500）+ Legacy 残骸段；同步命令/信号检测/板块边际量回填/板块回测四节、核心工作流第4条、字段说明均加 broken 标注。**关键修正**：用户最初提的"两层都写清"会被误解为两层都可用，实际是"唯一可用主库 + broken legacy 残骸"，CLAUDE.md 按后者措辞以防后续 agent 误删旧脚本或误跑期待出数据。可复用知识点：代码层表名残留 ≠ 数据库层物化表存在，迁移期文档须明确"唯一可用 + broken 待迁移"而非"两层并存"，否则会误导。
- 2026-07-06 · devin · 新增 Level2 大单资金流数据源模块（PR #135，分支 `data-source/l2-moneyflow`，待用户确认合并）：`scripts/moneyflow/` 四脚本——个股资金流图 + 昨日涨停榜 + 全市场成交额前100榜 + 量化单榜，数据源 ClickHouse `db.base32.cn`（share 库逐笔成交/快照，凭证走环境变量 CH_*）。口径：大单=同一委托单当日累计≥50万、主动方=委托编号大的一方（BuyNo>SellNo⇒主动买）、综合得分=(0.7×主买+0.3×总买)/流通市值×100、量化单=金额±1%窄带≥10笔簇（单笔≥200万）。**架构决策（OLAP 分工）**：原始逐笔 tick 留在 ClickHouse，DuckDB 只落每日计算结果——schema.sql 新增 `feature_l2_capital_flow_daily`（scan_type='limitup'|'top100'）与 `feature_l2_quant_orders_daily`，带 stock_ts_code 可与其它 feature_* 表 join；扫描后自动写入（write_to_duckdb.py，失败不影响榜单）。数据库红线：仅盘后运行、逐股限速0.3s、名单聚合分批(代码前缀4批)+重试+本地缓存。新增 `.claude/skills/l2-moneyflow/SKILL.md`。已用 07-06 实际榜单验证 DuckDB 回填与查询一致。
- 2026-07-06 · devin · 完成 2026-07-06 单日全量复盘（Devin 经 Cloudflare 隧道远程编排）：run_review_sync.py 全模块绿、质检 RESULT: COMPLETE（fact_stock_daily 5518 行、sector_stock 28269 行、limit_heat 119 题材）；生成段 intelligence.cli daily 14 步全 PASS；evolve_daily.sh 8 步 + agent-daily + winrate + workbench + cockpit 均完成。踩坑记录：① 隧道中途 530（cloudflared 掉线），用户在 Mac `launchctl kickstart -k` 重启后恢复，spawn.py 守护化的同步任务未被误杀（验证了 skill 的守护化方案）；② exec 非交互 shell 无 node PATH，cdp-proxy 需用绝对路径 /usr/local/Cellar/node/26.3.1/bin/node 启动；③ preflight 偶发误报 fupanhui 未登录（CDP proxy 刚起 WS 未就绪），重试即过；④ render_cockpit.py 参数已改为 --knowledge-root（SKILL.md 中 --kb-briefings-dir 已过时）；⑤ 知识库 wiki/briefings/2026-07-06.md 不存在，晨会简报步骤跳过。
- 2026-07-06 · devin · 驾驶舱接入 L2 资金流与每日四问统一 HTML 界面（PR #135 追加提交 c0f3496，同分支 data-source/l2-moneyflow）：新增 `scripts/render_moneyflow_html.py`（DuckDB feature_l2_capital_flow_daily/feature_l2_quant_orders_daily → 复盘/moneyflow/index.html，昨日涨停/成交额前100/量化单三页签 + 日期切换 + 表头排序，自包含单文件）与 `scripts/render_lifecycle_qa.py`（market_feature_store/exports/*-daily-agent.json 的 decision 分区 + logic_lifecycle → 复盘/lifecycle/index.html，每题材四问卡片：阶段/变化原因/证据状态/下一步）；`render_cockpit.py` 新增「大单资金流」「每日四问」两个 section/TOC/KPI 卡片；两页产物目录已 gitignore。用法：扫描/导出后运行 `python3 scripts/render_moneyflow_html.py` 与 `python3 scripts/render_lifecycle_qa.py`，再 render_cockpit 即可从总舱点入。已用 07-06 数据浏览器验证三页渲染正常。
- 2026-07-06 · devin · 「每日四问」语义纠偏（PR #135 提交 2d9ce08）：用户澄清每日四问=Codex/Claude 双盲问答的每日清单落库（docs/learning/forecast-review-ledger/），不是逻辑生命周期四问。已删 render_lifecycle_qa.py，改为 `scripts/render_dual_blind_qa.py`（台账 md 按 H2 切板块渲染 + manifest/answer/verdict 状态与命中率徽章 + 链接台账原文/双盲并排对比页 → 复盘/dualblind/index.html），render_cockpit 卡片同步。注意：后续 agent 见到「每日四问」默认指双盲台账。
- 2026-07-07 · devin · 训练闭环三断点修复（纯配置，无代码改动、无 PR）：① 统一共享台账——`FORESIGHT_USERS_DIR=/Users/a77/agent-memory/.foresight` 写入 `~/.zshrc`，原 `intelligence/users/linxiaoqi5111/` 四份 jsonl 已迁移（工作区旧目录改名 `linxiaoqi5111.migrated-to-foresight.bak` 防双轨），checkpoint status 从共享台账读到 4 条；② 夜间回检 launchd 已装——main 独立 clone `~/finance-workspace-recheck`（a2bb64d），build_plist.py 生成 `com.financeworkspace.checkpoint-recheck.plist`（03:50，env 带 FORESIGHT_USERS_DIR + SUBCONSCIOUS_VAULT=/Users/a77/agent-memory + KNOWLEDGE_WIKI=/Users/a77/knowledge-base-private/wiki，--db-path 指向工作区 duckdb），手动触发验证 LastExitStatus=0、落盘 2 条 verdict、人类可读日志 `agent-memory/可证伪点回检/2026-07-07.md`；③ red-team 不用新写——origin/main 已有 `intelligence/services/red_team.py` + CLI 子命令（同类历史纠偏+低胜率类别+固定反方骨架），在 recheck clone 实测可用。注意：工作区停在 `codex/docs/marketing-agent-chain-spec`（领先24/落后40），red-team 要在 `~/finance-workspace-recheck`（main）跑，或等工作区分支合并回 main。launchd 只读 plist 里的 env，不读 zshrc——改台账路径必须两处同步。

### 2026-07-07 · devin · 每日四问自动链上线
- launchd `com.financeworkspace.dual-blind-forecast`（每交易日 09:10，周末脚本内跳过）：
  `scripts/dual_blind_auto.sh` = manifest 冻结（视角日取 DuckDB max(trade_date)，依赖 09:00 deltapull）
  → codex exec（/Applications/Codex.app/Contents/Resources/codex，需 --skip-git-repo-check）
  → claude -p（~/.local/bin/claude，allowedTools 白名单：Read/Glob/Grep/Write/Edit/Bash(python3)）
  → validate 收卷 → index --html。答卷已存在则跳过（幂等），双盲纪律写死在 prompt。
- 18:30 全量复盘链（nightly_full_review.sh）末尾追加：最近 12 份答卷 recheck + index 重建（不阻断收尾）。
- 实测 2026-07-07 全链 2/2 答卷 validate 通过。commit 02e9a17 / bd6bbdb。
- agent 日志：logs/dual-blind.<date>.<agent>.log；链路日志：logs/dual-blind-forecast.out.log。

### 2026-07-07 · devin · 复盘收尾 Automation 上线
- Devin Automation `auto-79001292046b4d01b5fecbb28034189c`：工作日 19:15 北京时间（UTC 11:15）自动开会话，
  经 exec 隧道连 Mac 做清单 agent 侧收尾：扫回检 → Tier1 red-team → 双盲答卷状态 → 回补任务包分类 → 二选一偏好对，一条报告发用户。
- 依赖：18:30 daily-full-review 跑成（目前仍等 CDP proxy 迁移）；复盘失败时 Automation 只报原因不硬跑。

### 2026-07-08 · devin · 裁决闭环修缮（三仓联动，PR 已合并）
- 背景：审计发现判断类数据"只进不出"——verdicts 全 unverifiable（manual 无机检）、晨汇 Tier1 写判据不装闹钟、synthesis 279 篇零验证时点、进化.md/策略一次日验证是停更的早期实验。
- 金融仓 PR #145：checkpoint 新增 `market_daily` metric（`conditions=[{field,op,target}]`，字段白名单 `_FIELD_RE`、字符串简写 `advancers>=3000`）+ `MarketDailyResolver`（到期拉 `fact_market_daily` 当日行逐条比较，全部达标 hit/任一 miss/缺数 unverifiable）+ CLI `--metric-type market_daily --condition ...`；serenity-alpha 输出模板尾加 `## 验证清单`；进化.md 与策略一"次日验证"标 deprecated 指向 checkpoint 体系。
- 同日金融仓双盲链路继续分流：新增/调整 `scripts/dual_blind_flows.sh` 支持 `briefing|sellside` 两条答卷流，复用当日 manifest_sha，晨汇流读当日晨汇并按 T+1 回检，卖方流读视角日晚间 raw 研报并按 T+3 回检；`scripts/dual_blind_auto_verdict.py` 改为 source-aware verdict，按 `source=duckdb|briefing|sellside` 选择 stream/horizon/eval_date，最近有答卷日期幂等补裁，自动条目可刷新但非 `auto:` 人工裁决永不覆盖。后续排查双盲命中率时要按 source 分账，不要把卖方 T+3 缺数误判为漏裁。
- 知识库 PR #221：`docs/operations.md` Synthesis 第 5 条强制页尾验证清单（claim/verify_by/落空条件三字段）；morning-briefing Stage 5 加第 0 步"Tier 1 必落 checkpoint"（--source briefing_cross，能机检不落 manual）。
- 本仓 PR #10：corrections.jsonl 删模板占位行；两条市场路径 checkpoint（daa436/70138e）补 market_daily 规格。interactions 停更结论：采集没坏，潜意识模式 6/18 后未被调用。
- 可复用原则：判断类存储的质量复利=写入带可裁决结构+到期真裁决+结果回写；分界按句子类型（可被证伪与否）不按文档类型。

### 2026-07-08 · Agent Workbench 第 2 步：foresight 追问闭环（PR #151）

- 新增 `intelligence/services/followups.py`：回答后生成五类追问卡片（证据加深/反证验证/替代标的/盘面回检/题材迁移）。**选型**：LLM（复用 `llm_refine` provider 链）生成具体追问，无 key 时降级为五类模板——追问卡片永远出现，降级记 `llm_unavailable_template_followups`。与 foresight 区别：foresight 是"盘面驱动、每日主动发问"，followups 是"回答驱动、围绕本次答案追问"，共用同一降级哲学。
- `intelligence/api/app.py`：`_run_ask` 加 `s03 foresight_followups` step + `followups.json` 产物（单独 try，追问失败不拖死主答案——"增强体验与答案正确性解耦"的通用模式）；新增 `GET /api/runs/{id}/followups`。
- 前端 `index.html`：追问卡片点击 → `POST /api/runs {question, parent_run_id}`；子 run 显示「↳ 追问自」可回跳父 run。父子关联即 run 树，为后续会话/任务图打基础。
- 测试：`test_followups.py` + workbench API 用例，21 个相关测试全绿；本地无 key 端到端验证通过。PR base=`feat/workbench-p1-ask-api`（#150 未合并），#150 合并后需把 base 改 main。

### 2026-07-10 · Agent Workbench UI 重构与产物渐进迁移设计

- 用户确认采用“统一研究入口”：自由研究为首屏主动作，每日复盘、题材深挖、个股研究作为快捷工作流，共用 Run/Trace/Artifact/追问协议。
- 核心覆盖契约：所有面向 human reading 的 HTML/Markdown/结构化报告必须进入 Artifact Registry，能从 Workbench 搜索、打开并追溯 canonical 来源；Workbench 只做统一阅读层，不改变 `ledger-map` 的唯一写入者。
- 迁移策略：现有驾驶舱、复盘组合、Agent 简报、策略矩阵、双盲等 HTML 先零重写注册/嵌入；高频页面再按“研究回答 → daily-agent → 证据/记忆/回检”顺序原生化，功能等价前不删除旧渲染器。
- 技术选型：React + Vite 静态前端由现有 FastAPI 托管，继续复用 SSE；相比 Next.js，不引入第二套 Node 服务端运行时。右侧检查器默认展示证据、时点、L1-L4、缺口与降级，记忆展示本次命中的 corrections/experience cards，而非泛化成长等级。
- 设计文档：`docs/superpowers/specs/2026-07-10-agent-workbench-ui-redesign-design.md`，分支提交 `a233190`。当前停在 spec 用户审阅门，尚未进入实现。
- 2026-07-10 · devin · 强化跨机增量同步的 delta package 契约（PR #179，待 review/merge，**尚未合并**）：给 `db_delta_*` 增量包加 manifest v1 + checksum/schema/date/row/table-set 多维校验、安全解压（防 zip 路径穿越）、完整包 vs partial 包显式语义、zero-row 修正、导入失败原子回滚，并挂 data-quality CI。**可复用架构决策**：跨机数据同步的可靠性靠「自描述 manifest + 多维校验 + 原子回滚」，把「按名存在」升级为「内容经校验可信」；完整/partial 语义必须显式化，否则消费端会把部分包当整包合并。测试 1031 passed / 2 skipped、CI 全绿。**后续动作**：先 review/merge，再单独做历史遗留债务清洗与环境 blueprint。
- 2026-07-11 · codex · 用户确认 Workbench 采用 chat-first + Skill 模块化方向：Conversation 管长期消息，Run 管单轮执行，现有 `Run.session_id` 兼容映射 `conversation_id`；每轮重新跑 Ask/RAG 检索，并注入压缩会话上下文，不能长期复用首轮证据。Skill 采用“手动指定必执行 + 规则兜底 + LLM allowlist 自动补充”的混合路由，默认每轮最多 3 个；executor 先产出锁定数字/引用的结构化模块，LLM 只负责证据边界内的综合表达。PR #181 已覆盖首段结构化模块 SSE，本轮设计在其上扩展有序可重放消息流、持久会话与 Daily Review/Daily Agent 首批 Skill；旧 HTML 仅作原始报告。设计与执行表：`docs/superpowers/specs/2026-07-11-chat-first-skill-workbench-design.md`、`docs/superpowers/plans/2026-07-11-chat-first-skill-workbench.md`，提交 `6fb0ce0`。
- 2026-07-10 · devin · 完成双时态历史评测基础设施（finance PR #189，待用户确认合并）：`final_history` 使用当前最佳事实并显式标记 ex-post，`as_known_at` 只保留 cutoff 前可见行与 Git 资料；配对快照绑定源库签名/内容哈希，并拒绝运行中变化的 DuckDB。10 日 pilot 结果为 final 10/10 ready、PIT 8 partial/2 pending、截止与分区违规 0；20 日成交额窗口 0/10 eligible。扩量前需处理两个边界：07-02/07-03 已不再是无资料对照，以及当前库无 append-only revision log，因此冲突字段为 0 不等于历史上没有修订。
- 2026-07-11 · devin · 现状忠实度 / 历史重放 Phase 1 已落续作 PR #191（stacked on #189），但**未通过完成门**：固定 10 日登记为 8 份 canonical artifact + 2 日 `missing`；claim evaluator 对 JSON/Markdown/HTML 生成稳定 claim ID，显式区分 claim-level evidence 与 deterministic inferred mapping，后者可做 PIT 数字核验但绝不计入证据覆盖。a77 隔离 worktree 重跑时 DuckDB 签名前后不变；结果数字 323/325（99.3846%，另 4731 pending）、claim evidence 0/7368、cutoff 0/563，entity 1731 pending、fact/inference 7410 pending，10 份 gold 均为 candidate/0 approved，全部日期仍有版本缺口；`decision_eligible=false`、`phase_2_allowed=false`。**门控决策**：用户人工批准 gold/阈值前，不扩 50–100 日，不恢复预测优化；报告级引用或当前实体/成分不能回填为历史逐声明证据。
- 2026-07-11 · devin · 用户批准 Phase 1 后，PR #191 已追加 Phase 2 80 日扩量：task-plan 13 项门控通过，范围 2026-03-01～07-03，从 85 个交易日按月份/行情阶段/PIT 完整度确定性分层抽取 80 日；保留 25 份精确日期 canonical 报告和 55 个真实 missing negative control。新增独立 `inputs → claim evaluation → outcomes → compare-batch` 管线，input 不含 final、outcome 不含 as-known-at；a77 产物位于 `/Users/a77/fidelity-replay/phase2-v1-20260711/`，DuckDB 前后签名不变。结果：数字 333/335（99.4030%，39157 pending）、evidence 0/47779、cutoff 0/563；claims=333 matched / 2 mismatch / 7626 needs_review / 39968 unverifiable；PIT=20 partial / 60 pending，comparison=80 partial，gold=80 candidate / 0 approved，80/80 日期仍有版本缺口，故 `decision_eligible=false`。**可复用结论**：扩大样本验证了数值映射稳定性，但不能修复逐声明证据、历史版本或人工 gold 缺口；可复算与可溯源必须分开计分。
- 2026-07-11 · devin · 现状忠实度 P0/P1 已落 PR #192（stacked on #191）：新增 `claim-lineage-v1`，MarketAdapter 将 `source/updated_at` 沿 Theme Radar → logic-market-match → daily-agent 贯穿，canonical JSON 原生输出 `claims + evidence_catalog`，Markdown/HTML 从同一对象渲染。证据 ID 使用规范化 JSON 的内容哈希；原始 `table_row` 与派生 `derived` 分离，派生项必须带 `derivation.input_evidence_refs`，`scope=inferred` 仍不计 coverage，无真实时间戳不补造。新增 JSON + CLI + 静态 HTML Gold 工作台：双 reviewer、逐字段一致率、分歧自动 `needs_review`、显式 `I_APPROVE_GOLD`、approved 覆盖保护；从 Phase 2 exact-date reports 确定性分层选出 20 日/9 个 strata，待真人审核。a77 真实库 smoke（2026-07-10）产出 50 candidates、4316 evidence（derived 无输入=0）及 1066 daily-agent claims/819 evidence（无引用 claim=0）；全量测试 1080 passed / 2 skipped，CI registry/workbench 均绿。`decision_eligible=false` 保持不变；下一阶段是 PIT 冻结/版本化与两个 numeric mismatch 根因定位。
- 2026-07-11 · devin · PIT 冻结强化已落 PR #193（stacked on #192）：`pit-daily-snapshot/manifest-1.1` 给每行增加 `_pit.valid_time/known_at/captured_at/source_time`，仅纳入 `updated_at <= capture` 且 `< D+1` 的行；`source_update_time` 存在时标 `source_publication`，否则明确为 `ingestion_fallback`。新增双层 payload 校验、逐行时点边界校验和前序 manifest hash chain；finance/wiki 脏工作树成为显式 repository gap，并强制 `replay_eligible=false`，防止用 clean commit 冒充实际读取的未提交知识。a77 真实库 smoke：40,028/40,028 行携带 PIT metadata，138 行有源发布时间；现有 2026-07-10 v1.0 freeze 可被新 validator 向后兼容校验。测试 1082 passed / 2 skipped；旧历史仍不得因新版本机制而 retroactively 变成可证明。
- 2026-07-11 · devin · Phase 2 P3 已落 PR #194（stacked on #193）：两个 numeric mismatch 并非 rounding 个例，而是 `20d-plus-new-highs` 报告被全局表头规则错误映射到 `fact_stock_daily`；新增报告族 source contract，将涨幅/10日涨幅/成交额/市值绑定到 canonical `fact_stock_high_daily`，并从冻结源行读取真实 `known_at/updated_at`，不再用 valid date 伪装 source time。a77 重跑后 mismatch=0，但 916 条 new-high 数字声明因 canonical 源行在 cutoff 前不可见而全部转为 `unverifiable`，数字指标改为 0/0、pending 39,492；这说明此前 333/335 被跨表映射高估，不能继续作为有效结论。另新增 `phase2.negative-controls.json`，固化 55 个 exact-date missing report，确认其对五项 fidelity 分母贡献均为 0；`decision_eligible=false`。测试 1084 passed / 2 skipped，PR #193/#194 registry/workbench CI 均绿。
- 2026-07-11 · devin · Fidelity P4-A 生产渐进迁移已落 #195/#196 并部署 a77：#189→#194 已按依赖顺序合并；CI 从 `intelligence/tests` 扩到全量 Python（干净 venv 1147 passed），补齐固定版本 numpy/pandas/requests。生产代码软链 `/Users/a77/finance-workspace-runtime` 固定到 clean `main` commit `19c46ed`，新 20:05 job 先用 DuckDB 交易日硬门跳过周末/节假日，再从同一 commit 生成并校验 `claim-lineage-v1` theme-candidates、原子替换且保留备份，随后生成 daily-agent；20:30 PIT job 同样切到固定 runtime，数据/知识/日志目录独立。canary 对 2026-07-10 得到 50 candidates/4316 evidence、1066 claims/819 evidence，PIT v1.1 checksum/hash-chain validation 通过；`capture_ready=true` 但 wiki 脏树使 `replay_ready=false`。生产旧 07-10 artifact 不回写，仍为无 lineage + PIT v1.0；从下一交易日起前向积累，`decision_eligible=false`。**可复用决策**：代码根与可写数据根分离；“链路已运行”用 `capture_ready`，“输入可重放”用 `replay_ready`，不能混成单一上线状态。
- 2026-07-11 · devin · Chat-first Skill Workbench draft PR #190 已 rebase 最新 `main` 并整合 #185/#186 runtime assurance：legacy Run 与 Conversation turn 共用 bounded `RunSupervisor`，保留 timeout/shutdown/recovery；Conversation recovery 从 `session_id` 找回原消息、`skill_mode`/手动 Skill，并用 recovery event-ID prefix 防 SSE 重放冲突；queued/active cancel 都在响应前终态化 pending assistant message。UI 采用 Knevo 审计提炼的三栏 shell、composer 内 Skill picker 与 responsive drawer，但不复制 HTML/私有资源或后端不支持行为；仍保持 local/private。验证：Python 981 passed、Vitest 27 passed、Playwright 三视口 6 passed，PR CI 全绿且 mergeable clean；仍为 draft，未合并。

- 2026-07-11 · devin · PR #190 完成 Codex 风格的 Foresight 产品化适配（提交 `1f9ff14`）：不复制 Codex HTML/私有 CSS/assets，而把可复用产品语义映射为 Projects→唯一真实的本地「市场研究」工作区、Threads→Conversation、Composer→研究任务与 Skill 编排入口、Task Sidebar→证据/运行/记忆/回检及基于真实 Run 的步骤/证据/产物摘要。视觉从森林绿改为石墨/暖灰中性色 + 克制陶土橙，减少渐变与重阴影，保留成功/警告/失败语义色；Conversation/SSE/Run/Skill 协议和 local/private 边界不变。验证：ESLint、TypeScript、production build 通过，Vitest 27 passed，Playwright 桌面/平板/手机 6 passed；GitHub `workbench-check`/`registry-check` 全绿，PR 仍为 draft、mergeable clean。

- 2026-07-11 · devin · PR #190 新增默认托管 GLM + 会话级 BYOK（提交 `d41865a`）：普通用户只看到「Foresight 默认模型」，服务端优先读取 `FORESIGHT_BUILTIN_LLM_API_KEY`（兼容 `ZHIPU_API_KEY` / `GLM_API_KEY`）；高级设置支持 GLM/OpenAI/DeepSeek/Kimi/通义千问固定 HTTPS preset 和 model 覆盖。BYOK key 按 `user_id` 仅存 FastAPI 进程内存，`SecretStr`/`repr=False` 防调试回显，Conversation worker 通过 `ContextVar` 获取当前 turn provider，避免修改全局环境导致并发串号；服务重启或 DELETE 即清除，不进磁盘、Conversation、Run、artifact、trace、日志或 Git。首版不允许任意 Base URL，避免 SSRF；无服务端 key 仍保留确定性模板降级。验证：Python 1232 passed / 1 skipped / 4 subtests，Vitest 28 passed，Playwright 7 passed / 2 skipped，GitHub 两项 CI 全绿，PR draft/open/mergeable clean。

- 2026-07-11 · codex · 用户确认 Workbench-first 垂直金融 Agent 产品路线，并纠正阶段顺序为“双门制”：当前 P0 不是直接建设 10–30 人托管内测，而是先以 PR #190 为产品壳，通过 Self-use Maturity Gate——完成今日市场、题材研究、个股研究、消息解读、自选跟踪五个产品 Skill，统一数据新鲜度与真实数据质量，连续自用至少 10 个交易日且由用户本人明确认可；之后才进入 Hosted Beta Readiness，补认证、PostgreSQL 多租户、配额、公开证据网关、安全合规，再邀请 10–30 人。完整设计：`docs/superpowers/specs/2026-07-11-workbench-self-use-to-invite-beta-design.md`（提交 `190c2a1`）。**可复用决策**：founder/self-use product-quality gate 与 external beta readiness gate 分离，避免多租户基础设施掩盖核心产品尚不好用。

- 2026-07-11 · codex · Self-use Maturity 第一批实施计划已落：`docs/superpowers/plans/2026-07-11-self-use-baseline-and-maturity-gate.md`（提交 `8871aca`）。范围严格限定为 PR #190 最终审计、真实托管模型 smoke、私有自用事件 JSONL、10 交易日确定性成熟度门、CLI/Workbench 只读状态和自用 runbook；PR #190 转 ready/合并仍需用户单独授权。数据新鲜度、五个产品 Skill、今日情报/自选、质量评测拆成后续独立计划；外部认证/多租户不进入当前批次。
- 2026-07-11 · devin · Fidelity P4-B/C/D/E 已完成并上线：#197 双 cutoff/provenance/claim 完整性门、#198 content-addressed dirty-wiki delta、#199 150–300 条分层 Gold 双盲工装、#201 不可变 forward acceptance 台账均已合并。a77 生产软链固定 clean worktree `ea86010c`，三个 LaunchAgent 按 20:05→20:30→20:45 加载；7/11 非交易日 smoke 均 exit 0、未制造 acceptance 记录，窗口 summary 仍为 0 日且 `decision_eligible=false`。排障确认旧 remote-exec hostname 会在 connector 正常时仍返回 Cloudflare 530/1033，正确 ingress 是 `exec-a77.industry7view.com`，#202 已修正默认地址并合并。下一门：7/13–7/17 达到 5 个 accepted 日与至少 150 claims 后抽约 200 条 Gold；累计 10–20 个 accepted 交易日且双人审核完成后，才人工评估 eligibility。

- 2026-07-12 · codex · 质检已合并的 Finance [PR #203](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/203) 与 Knowledge Base [PR #268](https://github.com/linxiaoqi5111-del/knowledge-base-private/pull/268)：合并/CI 状态属实，RAG 110,065 全量构建与当时 `stale=0` 只证明构建时内容一致。发现两个启动前 P1：① Self-use gate 只统计不同 ISO 日期，未校验 A 股交易日、连续性、未来日期或 `recorded_at` 关系，测试样例自身包含周末，不能作为“连续 10 个交易日”门；② full index 在 `include_raw=true` 时会索引 `wiki` 同级 `raw/`，但 clean Git freshness 只哈希 `wiki` tree，后续只改 `raw/` 仍可能误报 `fresh`。另有 P2：Finance 对 stale/unknown RAG 命中只标 degraded、仍允许进入回答；Workbench 投影不持久化用户批准，`passed` 在 UI 中恒为 false。默认 `glm-5.2` 仍 429，故真实默认模型 smoke 未过、10 日 Self-use 计时不应启动。
- 2026-07-12 · devin · Self-use maturity 与正式消费者契约续作 [PR #204](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/204) 已合并至 `main`（`6b0602fa`），main `workbench-check` / `registry-check` 全绿：canonical calendar 使用 DuckDB `fact_stock_daily.trade_date`，非交易日/未来日期/日历不可用 fail closed；“必须连续”保留为显式产品 policy，默认不强制。event 必须绑定同 run 的 completed `report.json` 与 `report.complete`，且日期一致、`llm.used=true / zhipu / glm-5.2`、无 template fallback；identity `(run_id, trade_date, workflow)` 在同一文件锁事务内原子去重。正式 RAG 消费者 strict-by-default，stale/unknown 不进入 LLM；探索模式须显式开启并标 degraded。审批 ledger 绑定 eligibility fingerprint，成熟度变化使旧批准失效。验证：1428 passed / 1 skipped / 4 subtests、前端 lint/typecheck、pre-commit、GitHub 两项 CI 全绿。a77 三次专用 Workbench smoke（run 尾号 2356/2552/2725）均 completed、report completed、各有一个 `report.complete`、`llm.used=true / zhipu / glm-5.2`、无 fallback 字段、secret scan hit=0；旧 RAG snapshot 被 strict consumer 丢弃后整轮使用其他证据 degraded 完成，证明 fail-closed 不等于全轮失败。Knowledge Base `manifest:v1` stable release 已发布。**未完成**：不得提前写 Day 1；下一真实交易日才开始 Self-use 计时。
- 2026-07-12 · devin · Workbench 显式视角开关已落 [PR #205](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/205)：`neutral/single/compare` 与 `selected_perspective_ids` 贯穿 React → API → ConversationStore → recovery → TurnOrchestrator → Ask，并同时保存在用户/助手消息；复用 Perspective Lab 的用户私有 profile，KOL 原文按 profile 独立轻量 BM25 召回并作为观点层 system context，不进入事实 evidence；neutral 禁止 KOL/个人记忆，single 只加载一个 KOL，compare 固定数据中立先行并保留冲突，LLM 降级时明确“不代表 KOL 判断”。可复用决策：高风险领域的 persona/KOL 应采用「结构化模式开关 + 消息级审计 + 独立检索索引 + 事实/观点/推理分层」，而不是混入统一 RAG。验证 1402 Python / 35 Vitest / 9 Playwright，CI 两项全绿。
- 2026-07-12 · devin · 题材研究回答架构已落 [PR #211](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/211)：新增统一 `Claim`、`CompanyAssessment`、`ThemeResearchSpec` 与 `AnswerSpec`，检索层只召回候选，裁决层绑定公司与证据后确定已证实/候选/推测/缺失/冲突，表达层仅消费结构化契约；LLM 只可在 `AnswerSpec` 事实边界内组织语言，并在输出后拦截新增公司、数字和工程术语，无 LLM 时由确定性 presenter 按结论→公司表→缺口/反证→核验动作输出。稳定币、机器人、算力、低空经济共享 domain pack，不再增加题材专用 framing helper。验证：1473 Python passed / 1 skipped、38 Vitest、12 Playwright、pre-commit 与 GitHub 两项 CI 全绿。**可复用决策**：召回、证据裁决和表达必须以中间契约解耦，领域差异进入配置而不是模板分支。
- 2026-07-13 · codex · 用户授权后将 Workbench 输出质量堆叠链 [#221](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/221) → #220 → #219 → #218 → #217 → [#216](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/216) 自顶向下收拢并合入 `main@bb9754f`；最终 PR 树与 main 的 `registry-check` / `workbench-check` 均全绿。a77 canonical runtime 已原子切换到 clean 快照 `/Users/a77/.finance-runtime/finance-workspace-bb9754f248bf`，停止临时 8795，只保留 LaunchAgent 管理的 8792；Registry 从 2 个增至 6 个 Skill，`market_snapshot=false` 时 readiness 正确返回 503。**真实 UI 结论：架构/治理有改善，输出体验尚未过门。**同题材判断题由 `theme-research` 接管并在 227 秒后完成，但把整句问题当成题材名、注入无关 AI 算力/半导体主线、9 次重复加载向量模型且所有 narrow/broad/counter 检索为空，并触发弱证据确定性措辞警告；手动零 Skill 的 Base Finance 虽正确记录 `base_finance_fallback=true`，但 275 秒无正文后取消。下一 P0/P1 应做「结构化 intent/entity/timeframe/decision 抽取」「embedding 模型进程级复用 + 每轮检索共享」「空索引/strict 丢弃后 fail-fast」「流式检索阶段与 60 秒回答 SLA」，修复前不得把 P0～P2 合并等同于达到 Knevo 用户体感。
- 2026-07-15 · devin · Workbench 题材研究架构竖切已落 [PR #238](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/238)（stacked on #237）：组合式 `QueryEnvelope` 抽取主题、3–6 月期限与历史类比/情景树/反证操作符；沿既有 Owner DAG 将 definition、chain、company mapping、D6、D8、scenario、counterevidence 改为独立 deadline/failure policy 的 typed `StageArtifact`，共享单位从完整 `AskResult` 下沉为 adapter artifact。D6/D8 EvidenceAtom 会传入确定性情景树和证伪条件；RAG/公司映射失败时仍保留所需输出，但无公司级证据不提升公司候选、无来源数字概率被禁止、无 L3 时软化确定性措辞。a77 真实 DuckDB/RAG 原始 golden question 验证：company mapping 20 秒超时，D6/D8/scenario/counterevidence 均 completed，四个必需章节存在，证据来源仅 D6/D8，公司表为空；本地 1737 passed / 1 skipped、15 Playwright，PR 两项 CI 全绿。**可复用决策**：复杂研究能力优先采用「组合式意图 + 同一 DAG 的可独立缓存 typed stage + 能力守恒降级」，不要为每个题材复制 owner、AnswerSpec、EvidenceAtom 或 RAG。
- 2026-07-15 · devin · Phase 4 质量门禁前移已开 [PR #240](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/240)（stacked on #239→#238→#237，base `refactor/owner-stage-executors`，按约定未合并）：owner 级 `SkillOutput.status` 四态由 `_owner_result_status` 集中判定——contract 缺失或全部 required stages failed/timeout/skipped 才 failed，有可渲染 contract 但部分 stage 未完成判 partial（修正 handoff 记录的"过严判 failed"问题），证据门槛/AnswerSpec quality/warnings 决定 degraded；弱证据确定性措辞进入 claim policy 并新增 `weak_evidence_hard_certainty` 校验；CORE 公司晋级要求 company-bound verified claim + 硬证据层级 + 可解析硬层级 `EvidenceRef`，finalize 时不可解析的 CORE 降为 CANDIDATE，renderer 分渲染核心/候选公司。验证：本地 1752 passed / 1 skipped，PR 两项 CI 全绿；a77 真实数据三条 golden questions（stock-deep-dive / financial-analysis / news-impact）owner 状态全为 partial（首个 required 检索 stage 20 秒超时、后续 typed stages partial、contract 可渲染）；Linux 缺数据环境同三条问题 stock/financial 因无可追溯 contract 判 failed、news-impact 判 partial，无 stage 伪装 completed。**可复用决策**：质量门禁应前移为「只允许合法 claim 进入 renderer」的 policy 与 owner 四态裁决，而不是生成后 WARN；部分成功必须与整体失败区分（partial ≠ failed）。Phase 5 isolated sub-agent 按计划暂缓至 stage 契约稳定。
- 2026-07-17 · devin · compose 单调专项 + 架构改造第一批已合并（#263 黄金快照 e2e、#264 通用 Grounded Presenter、#265 Temporal Facts 读侧），a77 服务已切最新 main 并用真实 ask 验证：grounded 链路 `repaired`、无整篇模板降级、0 marker 泄漏。**根因结论**：模板化主因是旧 `present_llm_answer` 丢弃 LLM 措辞只回填 registry claim 原文，门禁整篇拒绝率其实很低（62 次合成仅 6 次降级）。方案 1（拆 ask.py）已开 stacked PR：[#266](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/266)（S/G/R 段抽出 `evidence_providers.py`）与 [#267](https://github.com/linxiaoqi5111-del/finance-workspace-private/pull/267)（W/E/L 段 + provider 单测），全量 1611 passed、黄金快照逐字一致，两 PR CI 全绿、待用户确认合并。**可复用决策**：拆上帝文件用绞杀者模式——先逐字搬段落进有清晰输入输出的 collect_* 函数（共享 `EvidenceContext`，`is_stale`/`stage_timeout` 等以回调注入保住测试 mock 点），黄金快照守护行为不变，registry/Protocol 归一留到行为等价确认后；D0~D9 数据块已有 ask_planner.BlockTask 插件形态，无需重复抽取。

## 2026-07-18 PR #268 验证
- #268（orphan marker 归一化）已合并，main CI 全绿（PR 上的 workbench-check failure 为 corepack 下载 pnpm 的网络抖动，与代码无关）。
- Mac runtime 已更新至 00f5d2b 并重启；真实 ask（液冷服务器）验证：grounded shadow status=repaired、presented_answer 有完整正文、不再出现 insufficient_grounded_body，最终 answer.md 即 grounded presenter 输出、0 marker 泄漏。
- 下一步：方案 3（skill 并行化 + 共享检索缓存）。

## 2026-07-18 方案 3 完成（PR #269）
- skill 改为共享线程池提前提交、按路由顺序消费；WORKBENCH_PARALLEL_SKILLS=0 回退串行；预算/超时/降级语义与输出确定性保持。
- 编排层最终 compose 的 AskOptions 补上 wiki_rag_cache_scope（与 owner skill 相同的 user:conversation scope），W 段向量检索命中 kb_rag 进程级 TTL 缓存。
- CI 全绿、1623 测试通过、黄金快照不变；Mac 已更新至 4c6816a 并真实 ask 验证（69s/59s vs 之前 71-72s，单 owner skill 路由下并行收益有限，多 skill 场景由单测覆盖）。
- 待办顺序剩余：方案 6B（存储迁 SQLite）→ 方案 2（LLM 检索 planner）。

- 2026-07-20 · codex · **Workbench 主链优化、单版本发布与真实 E2E 收口完成**：深研 P0/P1-B 全链合入后继续修复四类生产接缝——确定性头部路由先规范化 `QueryResolution` 再生成 `TurnIntent`，避免 legacy resolver 覆盖 market-watch/daily-review owner；语义闸门的成功丢弃改进 `diagnostics`，不再污染用户 warnings/degrade；新闻/个股 owner 按 `EvidenceRef.tier=L3` 判断官方硬证据而非误把 `L3` 当编号前缀，并把 Controller 已解析公司主体下传公告工具；CLI stdout 的 warning 与 JSON 分离，`[warn]`/无结果永不铸成 EvidenceAtom。本机补齐 Chrome debug IPv4 监听与 CDP proxy，外部 web 检索恢复；L3 当前只启用已验证的巨潮源，避免把缺 AkShare 的互动易 warning 当结果。最终 `main@0430d54471cf`、`origin/main` 与 clean canonical runtime `/Users/a77/.finance-runtime/finance-workspace-0430d54471cf` 一致，仅 LaunchAgent 8792 对外；clean-env 2108 passed / 1 skipped（8 条既有 utcnow 弃用警告）。真实生产矩阵全部零降级完成：daily `run_20260720_042635_031682`（23.0s）、theme `run_20260720_042703_788069`（55.7s）、news+巨潮原文 `run_20260720_042515_324864`（52.3s）、stock+5 条 L3 `run_20260720_043506_935713`（68.6s），均 GLM-5.2、secret/public scan=0；RAG worker ready/model_load_count=1。行情 readiness 为 PASS，但如实标记 requested=2026-07-18、served=2026-07-17、provider=duckdb_latest/historical。**结论边界**：已达到“本地日常可用 v1”，不等于完成产品成熟度认证；连续 10 个真实交易日 Self-use Gate 与用户本人认可仍须后续积累。

- 2026-07-22 · codex · **Task 0 能力单调性红线建立**：在分支 `fix/agent-harness-monotonicity`、commit `bd69e0ee` 新增离线 bare/current/episode 三臂 fixture 与可序列化结果记录；五项能力分固定为 `0..4`，总分按 `sum / 20` 归一化，若任一 harness 分数满足 `harness + 0.2 < bare` 则标记 `capability_regression`。关键词检查仅保留协议门，不进入能力分；semantic acceptance 默认缺少独立证据时标 `not_evaluated`，显式报告失败才阻断，保持既有 CLI 兼容。验证：Task 0 套件 12 passed，smoke/旧兼容套件 45 passed。后续 Task 2 真实 AgentEpisode runtime 尚未接入，本轮不声称能力提升。
